# Validation Rules Register

Validation rules are canonical server-side rules. Client-side validation may improve the experience but is never the only enforcement for a security, tenancy, permission or sovereignty rule.

| Validation ID | Entity / action | Rule | Severity | Server enforcement | Client UX | Message | Source | Tests | Status |
|---|---|---|---|---|---|---|---|---|---|
| VAL-AUTH-PAIR-001 | Sign-in credentials | A wrong pair is a form-level error, never a field error, and the message is identical for an unknown address and a wrong password | Block | Required | Alert beside the submit | Those credentials do not match our records. | ROLE_MODEL.md 9 | `SignInTest` | Implemented |
| VAL-AUTH-THROTTLE-001 | Sign-in attempts | At most 5 per address-and-network per minute | Block | Required | Countdown in the form alert | Too many sign-in attempts. Try again in N seconds. | NFR-SEC | `SignInTest` | Implemented |
| VAL-AUTH-STATE-001 | OIDC callback | `state` must match the session value and is single use | Block | Required | Generic sign-in failure | That sign-in link is no longer valid. | FR-AUTH-001 | `MicrosoftSignInTest` | Implemented |
| VAL-AUTH-NONCE-001 | OIDC ID token | The token's `nonce` must match the one this sign-in started with | Block | Required | Generic sign-in failure | Sign-in could not be completed. | FR-AUTH-001 | `MicrosoftSignInTest` | Implemented |
| VAL-NAV-POLICY-001 | Every gated node and route | A node names a policy; an unknown policy denies. The route is gated by the same policy as its sidebar entry | Block | Required, middleware | Node absent from the markup | 403 You do not have access to this area. | ROLE_MODEL.md 5 | `AccessModelTest` | Implemented |
| VAL-NAV-DOMAIN-001 | Business domain nodes | Access requires BOTH the minimum tier AND an entitlement to that domain. No tier implies a domain | Block | Required | Domain absent from the rail and from My Intelligence | Not shown | ROLE_MODEL.md 1 | `AccessModelTest` | Implemented |
| VAL-NAV-AUDITOR-001 | Compliance cluster | The Auditor capability satisfies the Compliance policy without conferring any operational tier | Allow | Required | Compliance cluster appears | Not shown | ROLE_MODEL.md 2 | `AccessModelTest` | Implemented |
| VAL-SOV-GEO-001 | Capacity / workspace / AI region | Storage and processing geography must be inside the approved list or carry an active exception | Block | Required | Preflight block | Selected region is outside the approved data boundary | REF-DPS-001 | TBD | Planned, Phase 02 |
| VAL-ORG-CODE-001 | Organisation code | Unique across the instance, and not mass-assignable so a change is a deliberate line of code | Block | Required, unique index plus a non-fillable attribute | Not editable in Release 1 gate 1 | Not shown | ADM-002 | `OrganisationScopeTest` | Implemented |
| VAL-ORG-CONTEXT-001 | Every organisation-scoped read and write | With no organisation context the scope matches NOTHING and a write is refused. Never "everything" | Block | Required, global scope plus a creating hook | An empty screen rather than another customer's rows | Not shown | CLAUDE.md, cross-organisation access denied by default | `OrganisationScopeTest` | Implemented |
| VAL-AUDIT-REDACT-001 | Every audit write | A sensitive key stores a placeholder; free text is swept for credential-shaped content; a long value is fingerprinted, never stored. There is no way to skip it | Block | Required, in the single writer | Not applicable | Not shown | CLAUDE.md, Release 1 section 3 | `RedactionTest`, `AuditLoggerTest` | Implemented |
| VAL-CORR-UUID-001 | Inbound `X-Correlation-Id` | Accepted only when it is a well-formed UUID; anything else is replaced | Block | Required, middleware | Not applicable | Not shown | ADM-024 | `AuditLoggerTest` | Implemented |
| VAL-SET-KNOWN-001 | System setting read and write | A key not declared in `config/platform.php` throws. A typo never resolves to a value | Block | Required | Not reachable from the form | Unknown setting "x" | ADM-021 | `SystemConfigurationTest` | Implemented |
| VAL-SET-NOSECRET-001 | System setting write | A key that reads as secret-bearing is refused, whatever the catalogue says | Block | Required | Not offered on the form | Setting "x" reads as secret-bearing | CLAUDE.md | `SystemConfigurationTest` | Implemented |
| VAL-SET-TIER-001 | System setting write | The actor must hold the tier the catalogue names. Checked in the writer as well as on the route, because console and queue callers pass neither | Block | Required | Screen not reachable | You do not have authority to change "x". | ADM-021 | `SystemConfigurationTest` | Implemented |
| VAL-SET-SCOPE-001 | System settings form | Only keys in the posted category are validated and applied; anything else is dropped | Block | Required, from the route segment | Not applicable | Not shown | ADM-021 | `SystemConfigurationTest` | Implemented |
| VAL-FLAG-KNOWN-001 | Feature flag | An undeclared flag READS as off and cannot be WRITTEN. Deleting a declaration must never turn a capability on | Block | Required | Not listed on the screen | That feature flag does not exist. | ADM-021 | `FeatureFlagTest` | Implemented |
| VAL-FLAG-PRECOND-001 | Feature flag | A switch declaring a precondition for a direction refuses that direction until it holds. Turning off local sign-in requires Microsoft sign-in to be configured | Block | Required | Alert above the table | Microsoft sign-in is not configured. Turning off local sign-in now would leave no way in. | ADM-021 | `FeatureFlagTest` | Implemented |
| VAL-ORG-DELETE-001 | Organisation | An organisation cannot be deleted while dependencies exist. There is no delete path at all: everything is scoped to it | Block | Structural, no route exists | No control offered | Not shown | ADM-002 | `OrganisationScopeTest` | Implemented |
| VAL-BU-CODE-001 | Business unit code | Unique within the organisation, normalised to uppercase so `fin` and `FIN` cannot both exist, and fixed once created | Block | Required, unique index plus a non-fillable attribute | Read-only after creation | A business unit with the code "X" already exists. | ADM-003 | `StructureAndReviewTest` | Implemented |
| VAL-BU-LOOP-001 | Business unit hierarchy | A unit cannot be its own parent nor sit under one of its own descendants. No relational constraint can express this, so the walk is in the service and is bounded | Block | Required | Own subtree excluded from the select | That would put "X" underneath one of its own sub-units. | ADM-003 | `StructureAndReviewTest` | Implemented |
| VAL-BU-INACTIVE-001 | Disabled business unit | Accepts no new team or assignment. Keeps everything already in it | Block | Required | Disabled units absent from the select | "X" is disabled and cannot take a new team. | ADM-003 | `StructureAndReviewTest` | Implemented |
| VAL-TEAM-BU-001 | Team | Belongs to exactly one business unit. Not nullable at any layer | Block | Required, NOT NULL plus a required field | Select has no empty option | Not shown | ADM-004 | `StructureAndReviewTest` | Implemented |
| VAL-USER-EMAIL-001 | Account email or UPN | Unique across the organisation | Block | Required, unique index plus a service check | Message on the email field | An account already exists for that email address. | ADM-005 | `UserRegistryTest` | Implemented |
| VAL-USER-DISABLED-001 | Account status | Only an ACTIVE account may authenticate. Invited, disabled, locked and expired may not - and hold no permission either, because a live session can outlive the moment somebody was disabled | Block | Required, both sign-in paths and the authorization layer | Generic sign-in failure | Those credentials do not match our records. | ADM-005 | `UserRegistryTest`, `AuthorizationTest` | Implemented |
| VAL-USER-WINDOW-001 | Access window | Access end must not precede access start, and a date outside the window refuses sign-in. Closing the window moves an active account to expired | Block | Required | Inline date validation | Access cannot end before it starts. | ADM-005 | `UserRegistryTest` | Implemented |
| VAL-USER-ELEVATE-001 | Any authority change | Nobody grants a tier above their own, acts on an account that outranks them, or delegates a permission they do not hold | Block | Required, in the services so console and review callers pass it too | Higher tiers absent from the select; ungrantable permissions shown disabled with the reason | You cannot give an account more authority than you hold yourself. | ADM-005, ADM-007 | `AuthorizationTest`, `RouteAuthorizationTest` | Implemented |
| VAL-USER-LASTADMIN-001 | Last System Administrator | Cannot be removed, disabled, locked or demoted. "Active" is the operative word: a disabled or expired administrator does not count | Block | Required, one guard called by every path | Controls hidden and the reason shown before the attempt | This is the last active System Administrator. | ADM-005 | `LastAdministratorTest` | Implemented |
| VAL-ROLE-SYSTEM-001 | Built-in roles | Code and tier are frozen, the role cannot be deleted or disabled. The display NAME stays editable | Block | Required | Fields read-only, delete absent | A built-in role cannot be deleted. | ADM-006 | `AuthorizationTest` | Implemented |
| VAL-ROLE-ASSIGNED-001 | Role deletion | A role somebody holds cannot be deleted. Remediation happens first, so each removal is recorded against the person it affected | Block | Required | Reason stated on the delete panel | This role is still assigned to N accounts. | ADM-006 | `AuthorizationTest` | Implemented |
| VAL-PERM-DENY-001 | Every permission check and grant | An unknown key DENIES on read and THROWS on write. A key removed from the registry stops granting on deploy | Block | Required, structural | Only declared permissions are offered | Unknown permission "x". | ADM-007 | `AuthorizationTest`, `NavigationIntegrityTest` | Implemented |
| VAL-ORG-SUBJECT-001 | Every mutation of an existing account or role | The subject must belong to the current organisation. Enforced in the SERVICE, so console commands, queued jobs, future APIs and the access-review applier are covered. An unplaced subject is refused too - treating "unknown owner" as "mine" is how a boundary check becomes a boundary hole | Block | Required, `UserRegistry::assertInOrganisation()` and `assertRoleInOrganisation()` | 404, and the record is absent from every list | Not shown. The denial is audited | CLAUDE.md, cross-organisation access denied by default | `CrossOrganisationMutationTest` | Implemented |
| VAL-AUTH-STATE-DISCLOSURE-001 | Sign-in refusal wording | The credential form returns one identical sentence for a wrong password, an unknown address and a suspended account. The Microsoft path names the person's own state, because their identity is already proven | Block | Required, both controllers | Generic on the form; specific after Microsoft | Those credentials do not match our records. / Your SemantIQ access has been disabled. | SEC-DEC-027, SEC-DEC-032 | `UserRegistryTest`, `MicrosoftSignInTest` | Implemented |
| VAL-SECPOL-KNOWN-001 | Security policy read and write | A key not declared in `config/security.php` throws. A typo never resolves to a value, because a security control silently taking its fallback path is worse than an error | Block | Required, in `SecurityPolicies` | Not reachable from the form | Unknown security policy "x". | ADM-009 to ADM-011 | `SecurityPolicyServiceTest` | Implemented |
| VAL-SECPOL-RANGE-001 | Security policy write | The catalogue's own rules are applied in the SERVICE, not only in the form request, so a console command or queued job cannot store an idle timeout of minus one | Block | Required | Inline field error | "Idle timeout, in minutes" was not accepted: ... | ADM-009 to ADM-011 | `SecurityPolicyServiceTest` | Implemented |
| VAL-SECPOL-REASON-001 | Security policy write, high-risk keys | A change to a key the catalogue marks high-risk requires a written reason. A policy weakened without an explanation is one nobody dares change back | Block | Required, in `SecurityPolicies::set()` after the before/after comparison | Reason box always shown; affected fields carry a "Needs a reason" badge | Changing "x" needs a reason. | Gate 3 rule 4 | `SecurityPolicyServiceTest` | Implemented |
| VAL-SECPOL-NOSECRET-001 | Security policy write | A secret-bearing KEY is refused whatever the catalogue says, and so is a credential-shaped VALUE. The refusal is audited without the value | Block | Required | Not offered on the form | That value looks like a credential. | CLAUDE.md | `SecurityPolicyServiceTest` | Implemented |
| VAL-AUTH-MODE-001 | Credential sign-in | The authentication mode decides whether the credential form is offered at all and who it admits. Under the default mode only a LOCAL System Administrator may use it, and the refusal is identical to a wrong password | Block | Required, route and `AuthenticationGuard` | The form is absent, not disabled | Those credentials do not match our records. | ADM-009, SEC-DEC-027 | `AuthenticationPolicyTest` | Implemented |
| VAL-AUTH-TENANT-001 | Microsoft sign-in | When an allowed tenant is set, the `tid` claim must match it. A MISSING claim is a refusal, not a pass: an allow-list that waves through what it cannot identify is not an allow-list | Block | Required, before the account is resolved | Generic refusal | This directory account cannot sign in to SemantIQ. | ADM-009 | `AuthenticationPolicyTest` | Implemented |
| VAL-AUTH-DOMAIN-001 | Microsoft sign-in | When an email domain allow-list is set, the part after the last `@` must match one entry exactly. A sub-domain is NOT implied by its parent | Block | Required, before the account is resolved | Generic refusal | This directory account cannot sign in to SemantIQ. | ADM-009 | `AuthenticationPolicyTest` | Implemented |
| VAL-AUTH-PROVISION-001 | Microsoft sign-in, unknown account | With automatic account creation off - the default - a directory account with no SemantIQ account is refused rather than given one | Block | Required, in `resolve()` | Named refusal, since Entra has proved who they are | You do not have a SemantIQ account yet. | ADM-009 | `AuthenticationPolicyTest` | Implemented |
| VAL-AUTH-DISCLOSURE-002 | Microsoft sign-in refused by policy | A refusal caused by the tenant or domain allow-list names NEITHER. SEC-DEC-032 permits telling somebody the state of their own account; it does not permit handing an outsider the shape of the customer's policy | Block | Required | Generic refusal, and the page carries neither value | This directory account cannot sign in to SemantIQ. | SEC-DEC-045 | `AuthenticationPolicyTest` | Implemented |
| VAL-SESSION-IDLE-001 | Every authenticated request | A session with no activity for longer than the idle timeout is ended, and the person is told which rule ended it | Block | Required, `EnforceSessionPolicy` | Redirect to sign-in with the reason | You were signed out after N minutes without activity. | ADM-010 | `SessionPolicyTest` | Implemented |
| VAL-SESSION-MAX-001 | Every authenticated request | A session is ended once it is older than the maximum duration, however active it has been | Block | Required, `EnforceSessionPolicy` | Redirect to sign-in with the reason | You were signed out because a session may last at most N minutes. | ADM-010 | `SessionPolicyTest` | Implemented |
| VAL-SESSION-CAPABILITY-001 | Session revocation and concurrency | Both need the session driver to list a person's sessions. Where it cannot, the capability reports UNAVAILABLE with the driver named, the action is not rendered, and the route refuses independently | Block | Required, `SecurityCapabilities` and `SessionRegistry` | Action absent; the limitation stated on the screen | Sessions could not be ended. This deployment stores sessions with the "file" driver ... | ADM-010, decision D3 | `SessionPolicyTest` | Implemented. **Unavailable on production** |
| VAL-REAUTH-FRESH-001 | Every critical action | The session must have proved identity within the configured window. A session with no recorded confirmation is NOT confirmed, even immediately after sign-in on a path that forgot to record one | Block | Required, `ConfirmIdentity` middleware | Redirect to the confirmation screen, naming the action | Confirm it is you. | ADM-010 | `SessionPolicyTest`, `SecurityAccessTest` | Implemented |
| VAL-API-PAYLOAD-001 | Every request | A declared content length above the configured limit is refused with a 413 before the body is parsed | Block | Required, `LimitRequestSize` | Not applicable | That request was larger than this application accepts. | ADM-011 | `ApiSecurityTest` | Implemented |
| VAL-SECRET-NOVALUE-001 | Secret reference write, every free-text field | A value the audit redactor would alter is refused - a bearer token, a JWT, an inline `secret=`, a connection string with a password. Checked in the request AND at the model, so a console command or queued job is covered. The message never quotes the value, and the value is removed from the flashed input so it is not written to the session or rendered back | Block | Required, both layers | Inline field error; the field is cleared | That looks like a credential. Record WHERE the credential is kept ... | ADM-012, CLAUDE.md | `SecretReferenceTest` | Implemented |
| VAL-SECRET-ROTATION-001 | Secret reference dates | The rotation date must fall on or before the expiry date. A reminder that arrives once the credential has lapsed is the one time it is useless | Block | Required | Inline field error | Rotation should fall on or before the expiry date. | ADM-012 | `SecretReferenceTest` | Implemented |
| VAL-SECSTORE-READY-001 | Every security policy read | With `security_policies` absent the catalogue default is returned. Not a fallback in the apologetic sense: with no table there can be no override, so the default IS the value in force, and the secure defaults stay authoritative | Allow | Required, `SecurityPolicies::get()` guarded by `SecurityStorage` | Screens render normally and say the values cannot be changed | Not shown | Deployment safety | `DeploymentOrderTest` | Implemented |
| VAL-SECSTORE-WRITE-001 | Every security policy or secret reference WRITE | With the table absent the write is refused before any other check runs. Nothing is written and nothing is audited as though it might have been. The message names the cause and the fix and quotes no SQL, no driver text and no table name | Block | Required, `SecurityPolicies::set()` and the `security-storage` middleware | Save button and New reference button ABSENT, not disabled | Security storage has not been initialised on this deployment ... Nothing has been changed. | Deployment safety | `DeploymentOrderTest` | Implemented |
| VAL-SECSTORE-STATE-001 | Every screen reading a gate 3 table | Where storage is absent the screen says so. It must NEVER render the empty state, and never a healthy one: "no references recorded" says the store exists and is empty, and "nothing expiring in the next 30 days" beside a tick is a false healthy at exactly the moment the screen cannot see the data | Block | Required, in each controller and view | "Migration required" and "Cannot be checked yet", both distinct from the empty states | Not shown | Gate 3 rule 9, deployment safety | `DeploymentOrderTest` | Implemented |
