# Security, Privacy & Sovereignty Decisions

Use this file for concise decisions. For major architecture decisions create a separate ADR under `doc/execution/decisions/` and link it here.

| Decision / Exception ID | Date | Phase | Decision | Reason / requirement | Data affected | Region impact | Control / compensating control | Approver | Expiry/review | Evidence | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SEC-DEC-001 | 2026-08-22 | 00 | The organisation scope fails closed: no active organisation means no rows | NFR-SEC-02. Matching everything turns one missing session into a full cross-tenant read | Every customer-owned entity | None | Global Eloquent scope emitting an impossible predicate rather than returning early, so the intent survives into the SQL | Engineering | Review if a legitimate unscoped read path is ever needed | `OrganisationBoundaryTest`, `ConfigurationModelBoundaryTest` | Active |
| SEC-DEC-002 | 2026-08-22 | 00 | Session guard identity lookups run with the organisation scope lifted | The context is derived from the user being resolved, so a scoped lookup matches nothing and no session can ever be established | `users` | None | Confined to `OrganisationAwareUserProvider`, reachable only by the guard and only with a session identifier, remember token or credentials the caller already holds. The `User` model keeps the scope everywhere else | Engineering | Review if a local credential guard is ever added | `app/Support/Tenancy/OrganisationAwareUserProvider.php`, `SignInTest` | Active |
| SEC-DEC-003 | 2026-08-22 | 00 | Sign-in resolves identity inside `withoutScoping` before tenancy is known | A returning person is identified before their organisation is known; a scoped query would match nothing and silently create a duplicate account on every sign-in | `users` | None | Confined to one method; the match is on `entra_object_id`, a value only Microsoft issues | Engineering | Review when organisation assignment moves into the sign-in flow | `MicrosoftSignInController::resolveAndSave()` | Active |
| SEC-DEC-004 | 2026-08-22 | 00 | Audit events are append-only, enforced at the model | NFR-COMP-01. A schema comment saying "append-only" is not a control | `audit_events` | None | Model refuses update and delete with a `LogicException`. The database grant is the backstop for direct SQL and is a Phase 08 deployment item | Engineering | Confirm the production grant before go-live | `ConfigurationDataModelTest` | Active, with a named gap: the production database grant is not yet restricted |
| SEC-DEC-005 | 2026-08-22 | 00 | Audit records hashes, not payloads | NFR-COMP-01 and the standard's data-minimisation rule. A change can be proven without the control plane holding a copy of customer configuration | `audit_events` | Reduces what must be geo-controlled | SHA-256 hex in fixed-width columns, so a truncated value is rejected by the schema | Engineering | None | Migration `2026_08_22_181000` | Active |
| SEC-DEC-006 | 2026-08-22 | 00 | Every permissive data-protection setting defaults to false and both geography lists default to null | Deny by default, per the sovereignty standard section 4 | `data_protection_profiles` | Blocks production activation until the customer states their geographies | Column defaults, restated as model defaults so an unsaved instance is deny-by-default too, with a test comparing the two | Engineering | Values due before Phase 02 provisioning (decision D5) | `ConfigurationDataModelTest` | Active |
| SEC-DEC-007 | 2026-08-22 | 00 | The data protection profile is versioned, never edited in place | The standard requires the profile to be versioned and audited. "Which policy was in force when this was provisioned" must stay answerable | `data_protection_profiles` | None | `publishFor()` demotes and promotes inside one transaction; superseded rows are kept | Engineering | Auditing of the publish action is due in work item W5 | `ConfigurationDataModelTest` | Active, with a named gap: publishing is not yet audited |
| SEC-DEC-008 | 2026-08-22 | 00 | Support data capture requires a future expiry, not just a flag | The standard permits support capture only as a time-bound exception | Diagnostics | None | `allowsSupportDataCapture()` returns false without a future expiry, so a forgotten flag closes itself | Engineering | None | `ConfigurationDataModelTest` | Active |
| SEC-DEC-009 | 2026-08-22 | 00 | Fabric items are held by reference: identifiers and observed state only, never contents | NFR-COMP-01. Item data stays in the customer's OneLake in their approved geography | `fabric_items` | Keeps customer data out of the control plane geography question entirely | Schema has no column able to hold item contents | Engineering | None | Migration `2026_08_22_183000` | Active |
| SEC-DEC-010 | 2026-08-22 | 00 | Fabric item identifiers are unique per organisation, not globally | Two tenants are separate identifier spaces. A global unique index would let one customer's row block another customer's insert, which is a cross-tenant coupling hiding inside a constraint | `fabric_items` | None | Composite unique index on `(organisation_id, item_id)` | Engineering | None | `ConfigurationDataModelTest` | Active |
| SEC-DEC-011 | 2026-08-22 | 00 | Help topics are deliberately not organisation-scoped | Help content is product documentation, identical for every customer, and holds no customer data. Scoping it would mean an identical copy per organisation and a maintenance burden that guarantees drift | `help_topics` | None | Anything customer-specific a topic shows, such as a redirect URI, is a token resolved at render time and never stored | Engineering | Revisit only if customer-specific help content is ever approved | `ConfigurationModelBoundaryTest` | Active |
| SEC-DEC-012 | 2026-08-22 | 00 | Workflow orchestration state may hold step markers and Microsoft resource identifiers, never customer business data | NFR-COMP-01. A resumable run needs to remember what it did, not what the data was | `workflow_runs.state` | Keeps a JSON column from becoming an unclassified data store | Documented on the column and the accessor; reviewed per workflow type as each is written | Engineering | Re-check as each workflow type lands from Phase 02 | Migration `2026_08_22_180000` | Active |
