# Security, Privacy and Sovereignty Decisions

| ID | Date | Phase | Decision | Reason | Control | Evidence | Status |
|---|---|---|---|---|---|---|---|
| SEC-DEC-001 | 2026-08-23 | 00 | A wrong sign-in returns one form-level message, byte-identical for an unknown address and a wrong password | Distinguishing them turns the form into a directory lookup that confirms who works somewhere | Single message asserted equal in a test | `SignInTest::the_failure_message_never_says_which_half_was_wrong` | Active |
| SEC-DEC-002 | 2026-08-23 | 00 | The Entra ID token is decoded, not verified against JWKS | OIDC Core 3.1.3.7 permits it when the token arrives directly from the token endpoint over TLS with client authentication, which is this flow. A public client would have to verify it | Reasoning recorded in the class comment | `MicrosoftSignInController` | Active |
| SEC-DEC-003 | 2026-08-23 | 00 | Sign-in failures never show their reason in the browser | Microsoft's `error_description` can name internal tenant policy; a token exchange failure can carry credential detail | Message and logged reason are separate; test drives a declined consent to the rendered page | `MicrosoftSignInTest` | Active |
| SEC-DEC-004 | 2026-08-23 | 00 | A route is gated by the same policy as its sidebar entry, in middleware | ROLE_MODEL.md 5: menu visibility is convenience only. A filtered rail does nothing about a typed URL | One `Navigation::allows()` decides both; a test asserts the two agree for every tier | `AccessModelTest::the_route_policy_and_the_rail_agree` | Active |
| SEC-DEC-005 | 2026-08-23 | 00 | A platform tier never implies a business domain | ROLE_MODEL.md 1 and 4: a System Administrator operates the platform and holds no Sales or HR data unless separately entitled | Domain entitlement is a separate table, ANDed into the policy check | `AccessModelTest::a_system_administrator_holds_no_business_domain_by_default` | Active |
| SEC-DEC-006 | 2026-08-23 | 00 | Auditor is a capability flag, not a tier | It reads compliance evidence while holding no operational rights. As a rung it would either carry power it must not have or miss the cluster it exists for | `users.is_auditor`, accepted only by policies that opt in | `AccessModelTest::an_auditor_reaches_compliance_without_holding_the_tier` | Active |
| SEC-DEC-007 | 2026-08-23 | 00 | A node the viewer cannot reach is absent from the markup, not dimmed | Anything in the HTML has already been sent to their browser, and a dimmed link still discloses that the feature exists | Server-side filtering; a test asserts a Viewer's page contains no administration markup | `ShellTest::a_viewer_never_receives_the_administration_markup_at_all` | Active |
| SEC-DEC-008 | 2026-08-23 | 00 | Home shows no metric, trend or recommendation | PHASE-00-UI-SHELL.md 3 forbids faking production insights. A plausible figure on a landing page is worse than none, because somebody will believe it | Every section is an empty state; KPI tiles read "Not available", never zero | `pages/home.blade.php` | Active |
| SEC-DEC-009 | 2026-08-23 | 00 | Fonts are self-hosted rather than fetched from Google | A sign-in page reaching a third party before it renders leaks a visit from every viewer's network and blocks first paint on a service outside our control | Four woff2 files served from the application | `resources/css/app.css` | Active |
| SEC-DEC-010 | 2026-08-23 | 00 | The control-plane hosting geography is unconfirmed | cPanel hosting details have not been recorded. It must be confirmed before go-live, not assumed | Recorded as an open item in the sovereignty register | `doc/context/DATA_SOVEREIGNTY_REGISTER.md` | **Resolved 25 August 2026: Singapore**, confirmed by the product owner. Backup geography and replication remain open - see SEC-DEC-036 |
| SEC-DEC-011 | 2026-08-24 | R1.1 | An organisation-scoped query with no context in force matches NOTHING, not everything | The wrong end of that choice is a screen showing another customer's rows; the right end is an empty screen somebody investigates. CLAUDE.md states it as "cross-organisation access is denied by default" | A `whereRaw('1 = 0')` in the global scope, and a `require()` that throws on write | `OrganisationScopeTest::a_scoped_read_returns_nothing_when_no_context_is_in_force` | Active |
| SEC-DEC-012 | 2026-08-24 | R1.1 | With two or more organisations present, the context refuses to resolve by itself | `first()` would silently pick one and run every scoped query in the request against the wrong customer. A multi-tenant instance must bind explicitly | `count() === 1` rather than `first()` | `OrganisationScopeTest::a_second_organisation_stops_the_context_resolving_by_itself` | Active |
| SEC-DEC-013 | 2026-08-24 | R1.1 | An audit event may be written with no organisation; nothing else may | A failed sign-in from an unknown address happens before any organisation resolves, and refusing to record it would lose the evidence an incident review needs. No customer business data is written to that table | `organisationIsOptional()`, false everywhere except `AuditEvent`, with the reason in its docblock | `AuditLoggerTest::a_system_action_with_no_signed_in_person_is_still_recorded` | Active |
| SEC-DEC-014 | 2026-08-24 | R1.1 | Redaction cannot be skipped by a caller. There is no parameter for it | The rule is only as good as its weakest caller, and a caller who believes a value is safe is a caller about to store a client secret | One `Redaction` class; the writer applies it unconditionally | `AuditLoggerTest::a_secret_handed_to_the_writer_never_reaches_the_table` | Active |
| SEC-DEC-015 | 2026-08-24 | R1.1 | A failed audit write logs and continues; it does not fail the action | Otherwise a full disk or a locked table can stop an administrator disabling a compromised account. The lost event is a real cost and is logged at error level | `try`/`catch` in `AuditLogger::record()` | `AuditLogger` | Active |
| SEC-DEC-016 | 2026-08-24 | R1.1 | Audit events are append only, enforced by throwing on update and delete | A row that can be updated is not evidence. A silent `false` would let a caller believe it had corrected something | Model `updating` and `deleting` hooks that throw; no `updated_at` column | `AuditLoggerTest::an_existing_event_cannot_be_changed` | Active |
| SEC-DEC-017 | 2026-08-24 | R1.1 | The health probe and diagnostics report the PRESENCE of a credential, never its value, and name drivers rather than hosts | These pages are the most natural place in an application for a credential to end up, because everything about them invites "just show me what is configured" | No configuration value is read into a variable; every external message goes through `Redaction::scrub()` | `DiagnosticsTest::it_never_shows_a_credential_or_a_host` | Active |
| SEC-DEC-018 | 2026-08-24 | R1.1 | An inbound `X-Correlation-Id` is replaced unless it is a well-formed UUID | The id is echoed into logs and onto a page, so an unvalidated caller-supplied string is a log-injection foothold, and there is no cost to replacing it | `Str::isUuid` check before it is accepted | `AuditLoggerTest::a_caller_supplied_correlation_id_is_only_accepted_when_it_is_a_uuid` | Active |
| SEC-DEC-019 | 2026-08-24 | R1.1 | A feature flag never decides WHO may do something, only WHETHER a capability exists | A switch labelled "sign-in" invites exactly the wrong reading. Authorisation stays with the tier, the permission and the domain entitlement | Nothing in the authorisation path reads a flag; the screen states the rule | `FeatureFlagTest::the_screen_says_a_flag_is_not_an_access_control` | Active |
| SEC-DEC-020 | 2026-08-24 | R1.1 | Administrator remains denied to the Administrator tier in gate 1, although the Release 1 matrix grants them read | Widening access is never the safe default, and the granular read grant belongs to gate 2's permission registry, where it can be expressed as a read permission rather than as a whole cluster | Unchanged `policy:system-admin` on every `/admin` route | `PlatformOverviewTest::it_stays_behind_the_system_administration_boundary` | Active, revisit at gate 2 |
| SEC-DEC-021 | 2026-08-24 | R1.1 | The append-only guarantee on `audit_events` is enforced at the model, and the model cannot enforce it against a mass delete | Eloquent events do not fire on `->query()->delete()` or a raw builder delete. Found while clearing local verification data, not by a test. The only real control is a database grant: INSERT and SELECT for the application user, no UPDATE or DELETE on this table | Model hooks stop the accident. **The database grant has NOT been applied to production** | `AuditLoggerTest::an_existing_event_cannot_be_deleted` covers the model path only | **CLOSED 25 August 2026.** The grant could not express this on MySQL; SEC-DEC-037's database triggers are the control, applied and proved in production |
| SEC-DEC-022 | 2026-08-24 | R1.2 | `users` carries an EXPLICIT organisation scope, not the global one every other scoped table uses | `users` is the authentication table and is loaded by id on every request. A global scope that fails closed there turns "no context resolved" into "nobody can sign in, including the administrator who would fix it". Fail-closed is right for reading data and wrong for the lookup that decides who you are | `User::scopeInCurrentOrganisation()`, which fails closed identically, called by every path that lists or administers accounts | `CrossOrganisationTest::the_user_registry_is_scoped_even_though_users_carries_no_global_scope` | Active |
| SEC-DEC-023 | 2026-08-24 | R1.2 | The tier is a CEILING that nothing raises. A role carrying a permission above its holder's tier is inert | Otherwise `user_roles` is a privilege-escalation table: assigning a powerful role would be a way to buy authority the tier was supposed to cap | `Authorization::effectiveFor()` intersects the union with the tier's ceiling before returning | `AuthorizationTest::a_role_carrying_a_permission_above_its_holders_tier_grants_nothing` | Active |
| SEC-DEC-024 | 2026-08-24 | R1.2 | A permission declares a ceiling AND a separate auto-grant tier | With one tier serving both, the effective set always collapsed to the tier defaults and an assigned role could add nothing - roles were decoration. Found by a test, not by review | `Permission::grantedFrom`, defaulting to the ceiling. `admin.roles.manage` is the one opt-in permission | `AuthorizationTest::a_role_grants_a_permission_the_tier_does_not_carry_by_itself` | Active |
| SEC-DEC-025 | 2026-08-24 | R1.2 | `admin.roles.manage` is not carried automatically below System Administrator | Editing what a role may do is how somebody quietly widens their own authority. An Administrator can hold it, but only if it is granted deliberately | Ceiling Administrator, auto-granted from System Administrator | `AuthorizationTest` | Active |
| SEC-DEC-026 | 2026-08-24 | R1.2 | A disabled account is refused at the route and in the authorization layer, not only at sign-in | A live session can outlive the moment somebody was disabled, so a sign-in-only check leaves them working until they log out | `Authorization::allows()` and `Navigation::allows()` both check the status first | `RouteAuthorizationTest::a_suspended_account_is_refused_even_with_the_highest_tier` | Active |
| SEC-DEC-027 | 2026-08-24 | R1.2 | **On the CREDENTIAL form only**, a disabled account's refusal is byte-identical to a wrong password and to an unknown address; the real reason goes only to the audit trail | Nobody has proved anything yet at that point, so naming the state would confirm to an anonymous visitor that the address belongs to a real person here and say what happened to them - account enumeration | One message asserted literally against three cases; the reason recorded as an audit event | `UserRegistryTest::the_refusal_never_says_the_account_is_disabled` | Active. **Corrected 25 August 2026**: the original wording did not scope this to the credential path and so misdescribed the Microsoft path. See SEC-DEC-032 |
| SEC-DEC-028 | 2026-08-24 | R1.2 | There is no `permissions` table; the catalogue is code | A permission an administrator can invent is not a permission, because nothing checks a key no line of code names. It would grant nothing while appearing to grant something | `PermissionRegistry`, with keys validated on write and on check | `AuthorizationTest::an_unknown_permission_denies` | Active |
| SEC-DEC-029 | 2026-08-24 | R1.2 | Changing a tier, adding a role and granting a business domain are three routes, three forms and three audit events | The trail must never blur "made them an administrator" with "gave them Finance". They are different decisions with different consequences | Three separate methods on `UserRegistry`, three permissions, three actions | `UserRegistryTest::a_tier_change_and_a_domain_grant_are_different_events` | Active |
| SEC-DEC-030 | 2026-08-24 | R1.2 | An access review's revocations are applied through `UserRegistry`, never by direct delete | A bulk operation that bypasses the rules is how a bulk operation becomes the way around the rules | `AccessReviewService::apply()` calls the same methods a manual change calls | `StructureAndReviewTest::applying_a_review_revokes_through_the_same_path_as_a_manual_change` | Active |
| SEC-DEC-031 | 2026-08-24 | R1.2 | An undecided access-review item is never treated as an implicit approval | A review where half the items were ignored is a finding. Folding it into the same shape as a finished one hides exactly that | `complete()` refuses while any item is pending | `StructureAndReviewTest::a_review_cannot_be_completed_while_anything_is_undecided` | Active |
| SEC-DEC-032 | 2026-08-25 | R1.2 | **On the MICROSOFT path, the person IS told the state of their own SemantIQ access** - disabled, locked, expired, or the date their window ended - and who to ask. This is deliberately different from the credential form | Microsoft has already authenticated the person and it is their own account, so naming their own state enumerates nothing: they can learn nothing about anybody but themselves. The alternative sends a contractor whose access ended yesterday to a help desk to be told what the screen could have said. Approved by the product owner, 25 August 2026 | `MicrosoftSignInController::accessDeniedMessage()`, one branch per state. Nothing about another account, and no configuration detail, is disclosed; the audit reason may be fuller than the sentence shown | `MicrosoftSignInTest` - four tests covering disabled, locked, the expiry date, and that no other account or credential appears on the rendered page | Active |
| SEC-DEC-033 | 2026-08-25 | R1.2 | **Every mutation of an existing account proves the subject belongs to the current organisation, in the SERVICE and not the controller** | `users` carries no global organisation scope (SEC-DEC-022), which moves the whole tenancy burden onto the write paths. Controller-only checks miss console commands, queued jobs, future APIs and the access-review applier. **The hole was real**: a test written to demonstrate it showed one organisation's System Administrator disabling another organisation's account by supplying its id | `UserRegistry::assertInOrganisation()` on all seven mutations, plus `assertRoleInOrganisation()`. Fails closed on an unplaced account too | `CrossOrganisationMutationTest` - 12 tests, service and HTTP | Active |
| SEC-DEC-034 | 2026-08-25 | R1.2 | A cross-organisation subject returns **404, not 403**, and the exception extends `DomainException` rather than `RuntimeException` | 404 because a 403 confirms the id exists and belongs to somebody, and the ids are sequential integers. `DomainException` because every controller here catches `RuntimeException` to show a form message - a tenancy violation must sail past those handlers rather than be swallowed into a friendly error, so the guard cannot be defeated by a forgotten catch block | `SubjectOutsideOrganisation implements HttpExceptionInterface`, status 404 | `CrossOrganisationMutationTest::every_mutation_route_refuses_a_cross_organisation_subject` | Active |
| SEC-DEC-035 | 2026-08-25 | R1.2 | An account created by Microsoft sign-in is **placed in an organisation at creation**, and the sign-in is refused if none can be resolved | An unplaced account is unmanageable under SEC-DEC-033: an administrator could never disable or entitle somebody who arrived through SSO. Found when the tenancy guard exposed it. On a multi-organisation instance the context deliberately does not resolve, and guessing would put a person in the wrong tenant | `MicrosoftSignInController::resolve()` stamps `organisation_id` on a new account only | `MicrosoftSignInTest::an_account_created_by_microsoft_sign_in_is_placed_in_an_organisation` | Active |
| SEC-DEC-036 | 2026-08-25 | R1.2 | The control plane is hosted in **Singapore**, its backups are in **Singapore**, and there is **no replication outside Singapore** | A server's country is not the same as its backups' country - backups routinely leave the country the server sits in, which would move this data out of Singapore without the server moving. All three were asked separately and all three came back Singapore | `DATA_SOVEREIGNTY_REGISTER.md` records SOV-001 and SOV-005 to 008 as VERIFIED Singapore-only | The product owner's confirmation, 25 August 2026, for all three: server Singapore, backups Singapore, no replication outside | **Closed 25 August 2026** |
| SEC-DEC-037 | 2026-08-25 | R1.2 | The `audit_events` append-only guarantee is enforced at the database by **triggers**, not by a privilege grant | MySQL has no DENY: privileges are additive, so `UPDATE`/`DELETE` cannot be revoked on one table from a user holding them database-wide. Making grants work would mean revoking everything and re-granting table by table, and a table created by the next migration would carry no privileges at all. A trigger binds regardless of privileges and survives every migration | Two `BEFORE UPDATE` / `BEFORE DELETE` triggers raising SQLSTATE 45000. Applied manually in production; not in a migration, because a migration that creates them could also drop them | Applied to production 25 August 2026 and proved: `SHOW TRIGGERS` lists both, and `UPDATE audit_events SET action = action LIMIT 1;` returns `#1644 - audit_events is append only: rows cannot be updated` | **Active** |
| SEC-DEC-038 | 2026-08-25 | R1.2 | The seven-year retention sweep, when built, must drop the audit triggers, delete, and recreate them - as a deliberate, separately approved operation | The triggers make routine deletion impossible, which is the point. Retention still has to happen eventually, and the way it happens must be visible rather than a standing capability the application holds | Not yet built. Recorded so the constraint is designed for rather than discovered | None yet | **Open, by design** |
| SEC-DEC-039 | 2026-08-25 | R1.2 | **If the `audit_events` table is ever rebuilt, the triggers must be re-applied by hand** | Triggers belong to the table. `migrate:rollback` drops the table and takes them with it, and re-running the migration recreates the table WITHOUT them - the protection would be silently gone while every screen looked normal. They are deliberately not in a migration, because a migration that creates them could also drop them | Recorded here and in the verification document. Any operation that rebuilds `audit_events` on production must be followed by re-applying both triggers and re-running the proof test | None yet - no rebuild has happened | **Active constraint** |
| SEC-DEC-040 | 2026-08-25 | R1.2 | The triggers are DEFINER-bound to the application's database user | A cPanel-created user is what phpMyAdmin runs as, so this was not a choice. If that database user is ever dropped and recreated the triggers may stop resolving, so the proof test is part of any credential-rotation runbook | The proof test in the verification document | The application user, name held only on the server | **Active, low risk** |
| SEC-DEC-041 | 2026-08-25 | R1.2 | The **applicable privacy regime is the Singapore PDPA**, determined separately from the hosting geography | Singapore hosting makes the PDPA the likely regime, but CLAUDE.md is explicit that legal applicability must be confirmed before production acceptance rather than assumed by code. Where the data sits is a fact; which law governs it is a legal determination about the customer, their sector and their own contracts | Engineering continues to apply privacy-by-design, least privilege and the sovereignty controls regardless | The product owner's confirmation, 25 August 2026 | **RESOLVED: the Singapore PDPA applies.** DEC-002 traces four obligations it creates that this application does not yet meet |
| SEC-DEC-042 | 2026-08-25 | R1.2 | Knowing the regime turns three carried assumptions into **gaps**, recorded rather than closed | An applicability determination with no obligations traced to it is a line in a register, not compliance. DEC-002 names them: no way to answer an access or correction request; no breach-notification workflow against a three-calendar-day deadline; and a seven-year retention baseline that is now a position needing a stated basis per data category, not one number applied to everything | All three land in gate 4 alongside ADM-014 and ADM-015, where data protection policy belongs | DEC-002 | **Open, planned for gate 4** |
| SEC-DEC-043 | 2026-08-25 | R1.2 | `organisations.privacy_contact` is optional; the PDPA expects a designated contact to be available | Making it required changes an existing screen's validation and would fail for any organisation already saved without one. **Not changed unilaterally** - it is the product owner's call, and gate 4 is the natural place alongside the rest of the data protection profile | Raised in DEC-002 | Awaiting the product owner | **RESOLVED 24 August 2026.** The product owner approved a REQUIRED and STRUCTURED privacy contact - name, email, optional phone, optional role or title - with identification of existing null rows, a safe backfill, staged validation so nothing breaks until somebody saves, and an audit of every change. Built in R1.4a |
| SEC-DEC-044 | 2026-08-26 | R1.3 | **The security policy key namespace is shaped by the audit redactor.** Keys use the prefixes `sign_in.`, `activity.` and `api.` rather than the obvious `auth.` and `session.`, the secret column is `reference_type` rather than `secret_type`, and the revocation audit summary key is `ended_count` rather than `sessions_ended` | `Redaction::isSensitiveKey()` matches the fragments `auth`, `session`, `secret` and `key` anywhere in a name, and the same list drives `Redaction::summarise()`. A policy stored under an obvious name would have recorded every change as "[redacted] -> [redacted]" - degrading the trail for precisely the settings an auditor comes looking for - and `SystemSettings::set()` would have refused the write outright. **Two of the three were found by tests, not by review**: the revocation count came back as "[redacted]" in an assertion | A catalogue comment stating the rule, and a structural test asserting no declared policy key trips the redactor | `SecurityPolicyServiceTest::every_catalogue_key_survives_the_audit_redactor`, `::the_audit_summary_of_a_policy_change_is_readable_rather_than_redacted`, `SessionPolicyTest::revocation_ends_every_session_under_the_database_driver` | Active |
| SEC-DEC-045 | 2026-08-26 | R1.3 | **A Microsoft sign-in refused by the tenant or email-domain allow-list names neither.** The refusal is generic, unlike SEC-DEC-032's disclosure | SEC-DEC-032 permits telling somebody the state of THEIR OWN SemantIQ account, because Entra has already proved who they are and they can learn nothing about anybody else. This is a different question: the person is outside the customer's directory or domain policy, and naming the allowed tenant or the allowed domains would hand an outsider the shape of the customer's own security policy. The two rules are consistent because they answer different questions | `MicrosoftSignInController`, one generic sentence; the full reason goes to the audit trail | `AuthenticationPolicyTest::the_refusal_never_names_the_allowed_tenant_or_domains` | Active |
| SEC-DEC-046 | 2026-08-26 | R1.3 | **Security policy lives in its own table with its own catalogue, not in `system_settings`** | Decision D1, approved by the product owner 25 August 2026. `SystemSettings::set()` refuses every natural key for these features (SEC-DEC-044), so reuse would have meant contorting names to dodge a security guard and leaving a trap for the next person. The store also needs two things `system_settings` does not have: a compulsory reason on a high-risk change, and a capability requirement that says whether a stored value is actually in force. **This deviates from the release plan's table list**, which named `security_policies` AND `session_policies`; splitting one key/value store by subject matter would have doubled the machinery for fourteen values | `security_policies`, `config/security.php`, `SecurityPolicies` | `SecurityPolicyServiceTest` | Active |
| SEC-DEC-047 | 2026-08-26 | R1.3 | **ADM-011 is a live verification screen, not a settings page** | Decision D2, approved 25 August 2026. Release 1 adds no public API surface, so switches for an API that does not exist would be values nothing reads - the "unwanted parts hanging" problem by definition. ADM-011's own text is a list of CONTROLS, and the useful thing to do with controls is check them. **Two of the eight failed when the class was written**, which is the point of the screen | `ApiSecurityAudit` reads the route table, the middleware stack, the redactor and the policy values. A control that cannot be established returns `NotVerified` and never `Healthy` | `ApiSecurityTest` - eleven tests including one that turns a control off and asserts the screen notices | Active |
| SEC-DEC-048 | 2026-08-26 | R1.3 | **HSTS ships OFF, its duration ships at one day, and the Content Security Policy ships report-only** | HSTS is the only header here that cannot be taken back: a browser that has seen it refuses plain HTTP to this host for the whole max-age whatever the server later sends, so a wrong value is not a setting somebody can switch off but an outage nobody can shorten. A short max-age is the mistake you can recover from within a day. An enforcing CSP that is slightly wrong breaks the shell for everybody at once, so the safe order is report, read, then enforce | `SecurityHeaders`, and the catalogue defaults | `ApiSecurityTest::hsts_is_not_sent_by_default`, `::hsts_is_still_not_sent_over_plain_http_when_switched_on` | Active. **Enabling HSTS on production remains a separate approval** |
| SEC-DEC-049 | 2026-08-26 | R1.3 | **Session revocation and concurrent session limits are BUILT but report themselves unavailable, and production is not changed** | Decision D3, approved 25 August 2026. Production runs `SESSION_DRIVER=file`; file sessions are opaque blobs with no index by user, and Laravel's `logoutOtherDevices` is no escape because it rehashes a password a federated account does not have. Building the capability and reporting the environment honestly is better than either shipping a broken button or deferring the feature: the code is tested under both drivers, and the prerequisite is named on the screen rather than in a document | `SecurityCapabilities` answers once for the whole application; the revocation ACTION is absent rather than disabled (gate 3 rule 10) and the route refuses independently | `SessionPolicyTest` - both drivers, including that revocation returns null rather than zero when it cannot run | Active. **Switching the driver is a separate approved change** |
| SEC-DEC-050 | 2026-08-26 | R1.3 | **Re-authentication stores a timestamp and nothing else. No extra Microsoft token, no cached credential** | A federated account has no password here, and inventing one would be exactly the credential this application is designed not to hold. The Entra round trip with `prompt=login` IS the proof. A re-authentication mechanism that accumulated Microsoft tokens would have made the control plane a more attractive target than the control was worth | `Reauthentication` writes one ISO timestamp into the session. The confirmation lives in the SESSION, not on `users`, so confirming on a laptop does not unlock a critical action on a phone | `SessionPolicyTest` - eight tests covering both paths, expiry, and the identity check on the returning account | Active |
| SEC-DEC-051 | 2026-08-26 | R1.3 | **A refused credential is removed from the flashed input, not merely rejected** | A failed form request redirects back WITH INPUT and the form repopulates from it, so a credential-shaped value that the validator refused was still being written to the server's session store and rendered straight back into the next page's HTML - the exact outcome the check exists to prevent. Merging into the form request is not enough: it is a COPY of the request and the exception handler flashes the original. **Found by a test that looked for the value on the page afterwards**, rather than only asserting that the save was refused | `StoreSecretReferenceRequest::failedValidation()` builds the redirect itself with the offending fields blanked and everything else preserved | `SecretReferenceTest::the_refusal_never_quotes_the_value_it_refused` | Active |
| SEC-DEC-052 | 2026-08-26 | R1.3 | **Reading the secret-reference list is as restricted as writing it** | The rows hold no credential, but a list of every credential a system depends on, where each one lives and when it lapses, is a map for anybody attacking it. Treating the read as the harmless half is how a targeting list becomes widely readable. `admin-secrets` is also a SEPARATE navigation policy from `admin-security`, even though both resolve to System Administrator today, so a later decision to delegate policy reading cannot hand the credential map over with it | Both permissions at System Administrator, ceiling and auto-grant alike | `SecretReferenceTest::an_administrator_below_system_administrator_cannot_reach_the_screen` | Active |
| SEC-DEC-053 | 2026-08-26 | R1.3 | **Laravel's `storage.local` routes are accepted as anonymous only while the local disk is private, and the condition is CHECKED** | Those routes carry no middleware at all and a naive control reports them as an anonymous hole. They are not one: `Illuminate\Filesystem\ServeFile` refuses anything without a valid relative signature while the disk visibility is private. Setting it to `public` turns that check off and makes them exactly the hole they first looked like - so the ADM-011 authentication control tests the condition rather than allow-listing the route names unconditionally | `ApiSecurityAudit::authenticationEnforcement()` | `ApiSecurityTest::making_the_local_disk_public_turns_the_storage_routes_into_a_finding` | Active |
| SEC-DEC-054 | 2026-08-26 | R1.3 | **The default authentication mode refuses the credential form to any local account below System Administrator**, and the screen says how many accounts that is | This is what ADM-009's "Require SSO for Business Users" MEANS, and it is also the change most likely to surprise somebody the morning after a deployment. A policy screen that makes the change without stating its blast radius is a policy screen that causes an incident | `AuthenticationGuard::credentialRefusal()`; the count is shown on the Authentication Policy screen as a warning | `AuthenticationPolicyTest::the_default_mode_refuses_a_local_business_user_with_the_same_message`, `::the_screen_counts_the_accounts_the_current_mode_shuts_out` | Active. **Check this count before merging to production** |
| SEC-DEC-055 | 2026-08-26 | R1.3 | **Security policy READS fall back to the catalogue default when the table does not exist; WRITES fail closed** | The deploy workflow ships code and does not run migrations, so every release adding a table opens a window. Gate 3 middleware runs on the WEB stack - `SecurityHeaders` reads a policy on every response - so before this fix the first request after a deploy returned **500 on `/sign-in`**, taking the whole site down with nobody able to sign in to notice. Measured with a test, not predicted. The two halves differ because a read has a correct answer available (with no table there can be no override, so the default IS in force) and a write does not: accepting one and discarding it would tell an administrator their security policy had changed when nothing had | `SecurityStorage`, guarding `SecurityPolicies::get()` and `::set()` | `DeploymentOrderTest` - 21 tests including a full up-down-up round trip | Active |
| SEC-DEC-056 | 2026-08-26 | R1.3 | **Schema-not-ready is detected with a schema question, never by catching a database exception** | Catching one and falling back would also swallow a broken connection, a permissions problem or a corrupt table, and report all three as "everything is fine, using defaults" - which is precisely the failure mode a security control must not have. `Schema::hasTable()` asks one specific question and answers only that; everything else still fails loudly | `SecurityStorage::tableExists()`. Singleton and memoised, so it costs one query per table per request rather than one per consumer | `DeploymentOrderTest::readiness_is_answered_by_a_schema_question_and_not_by_swallowing_an_error` | Active |
| SEC-DEC-057 | 2026-08-26 | R1.3 | **A screen with no storage shows a distinct state, never the empty one and never a healthy one** | "No secret references recorded" says the store exists and is empty - a different and far more comforting fact than "we cannot tell you what is recorded". The Expiring credentials panel was worse: a green tick and "nothing expiring in the next 30 days" is a false healthy about the one thing on that page that can take an integration down, at exactly the moment the screen cannot see the data. Both were caught by LOOKING at the rendered page | "Migration required" and "Cannot be checked yet"; secret posture reads `NotVerified`; the storage gap is the first configuration gap listed | `DeploymentOrderTest::the_secret_reference_index_shows_migration_required_and_not_an_empty_list`, `::the_expiring_credentials_panel_does_not_show_a_green_nothing_expiring` | Active |
| SEC-DEC-058 | 2026-08-26 | R1.3 | **The four secret-reference routes carrying an id take a plain integer, not an implicit model binding** | Laravel's `SubstituteBindings` lives in the `web` middleware GROUP, which runs before any route-level middleware - so an implicit binding queried `secret_references` before the storage guard could refuse, and a typed URL during a deployment window still returned a raw database error. A guard that runs after the query it is guarding is not a guard. Found by the test that walks all five routes with the table dropped | `whereNumber()` on the routes; `SecretReferenceController::find()` resolves and 404s, which also makes the organisation boundary explicit rather than inherited | `DeploymentOrderTest::every_secret_reference_write_route_is_refused` | Active |
| SEC-DEC-059 | 2026-08-26 | R1.3 follow-up | **"Nothing is being tracked" is reported as Not Configured, never as Healthy** | Found by the product owner on the LIVE site. With zero secret references the Expiring Credentials panel showed a green tick and "Nothing expiring in the next 30 days" - true, and read as reassurance about an estate nobody was watching. Zero tracked and zero expiring produce the same empty list and mean opposite things. Healthy is appropriate only once something IS tracked and none of it is near expiry | Three cases in the panel rather than two: storage absent, nothing tracked, nothing near expiry. The label comes from `SecurityStatus::NotConfigured->label()` so it cannot drift back into reassurance | `SecurityOverviewTest::nothing_tracked_reads_as_not_configured_and_never_as_healthy`, plus its counterpart asserting Healthy DOES return once a reference exists | Active |
| SEC-DEC-060 | 2026-08-26 | R1.3 follow-up | **The posture explanation is calculated from the same result as the badge, never written as fixed text** | Also found on the live site. The summary read "One critical finding among four healthy areas is a critical posture" - an explanation of the RULE that read as a description of the STATE - while the badge said Warning. A static explanation of a calculated value will eventually contradict it. The fix is not better wording: it is deriving both from one memoised `areas()` result so they are incapable of disagreeing, and naming the areas that set the level so the sentence is useful as well as true | `SecurityPosture::areas()`, `::overall()` and `::overallExplanation()`. Memoising also stops the eight-control ADM-011 audit running three times per page load | `SecurityOverviewTest::the_posture_explanation_never_contradicts_its_own_badge`, driven across four real postures | Active |
| SEC-DEC-061 | 2026-08-26 | R1.3 follow-up | **Browser verification of a new screen must include the day-one empty state, not only the state that shows the feature working** | Both defects above survived 405 tests and 25 browser combinations because the verification ran against a SEEDED database. Three secret references meant the panel took its populated branch and the posture reached Critical - the one state where the fixed sentence happened to be accurate. Seeding a database to demonstrate a screen hides exactly the states a fresh install starts in, and a fresh install is what every customer sees first | Recorded as a rule for gates 4 to 7 | The two tests above are both written against the empty state | Active |
| SEC-DEC-062 | 2026-08-24 | R1.4 | **`Authorization` learns an explicit Auditor capability. A permission may declare `orAuditor`, and `users.is_auditor` becomes an authorization fact rather than a navigation-only one** | Decision D2, approved 24 August 2026, option (a). Three sources disagreed: `admin.audit.view` was declared at System Administrator, the navigation rail admitted the Compliance cluster at Domain Owner or Auditor, and `ROLE_MODEL.md` says an Auditor reads the audit trail. `is_auditor` was understood by `Navigation` and not by `Authorization`, so an Auditor could SEE the leaf and be refused the screen - and worse, the rail was the only thing standing between a typed URL and the trail. CLAUDE.md is explicit that hiding a menu item is never authorization. **This changes gate 2's authorization core and is treated as security-critical** | `Permission::orAuditor`, honoured by `Authorization::effectiveFor()`. The capability grants audit READ only: it carries no business-domain entitlement and no governance write or approve permission, and it does not raise the tier ceiling | Explicit authorization tests, including a typed URL reaching the audit route server-side, an Auditor refused every governance write, and an Auditor holding no Finance, Sales or People data | Active |
| SEC-DEC-063 | 2026-08-24 | R1.4 | **`audit_events.ip_address` sits behind its own permission, `admin.audit.view_network`, at System Administrator only** | Decision D8, approved 24 August 2026. An IP address is personal data and is rarely what an audit reader actually needs. Bundling it into `admin.audit.view` would mean the Auditor capability SEC-DEC-062 just created hands out network identifiers as a side effect of reading the trail | A separate permission, ceiling and auto-grant both System Administrator. The column is not selected, not rendered and not filterable without it - absent rather than masked, so the page does not advertise what it is withholding | A test asserting the value does not appear in the response body for an ordinary audit viewer or for an Auditor | Active |
| SEC-DEC-064 | 2026-08-24 | R1.4 | **A PDPA-01 access response describes Band C attribution rather than naming the other person. Default `describe`, narrowable by a reviewer, never widenable without a second approval** | Decision D5, approved 24 August 2026. "Alice granted Bob the Finance entitlement" is personal data about two people, and an access request from one of them is not consent from the other. Releasing somebody else's identity in answer to a subject access request is the worst failure available in this gate. Widening is the dangerous direction, so it is the direction that needs a second pair of eyes | Three treatments per band - `include`, `describe`, `exclude`. Band C defaults to `describe`, which yields the action and the date and not the actor. A reviewer may move a band toward less disclosure alone; moving it toward more requires a second approver | Tests asserting another person's identity never appears in an assembled response, and that a widening without a second approval is refused | Active |
| SEC-DEC-065 | 2026-08-24 | R1.4 | **Data Protection and Data Sovereignty profiles are versioned and immutable once approved. A change writes a new version and supersedes the old** | Decision D4, approved 24 August 2026. A mutable row cannot answer "what was in force in March", which is exactly the question a regulator, an auditor or a breach assessment asks. A sovereignty position that can be edited after the fact is not evidence | `version`, `status` (draft, approved, superseded), `approved_at`, `approved_by_user_id`, `superseded_at`, `superseded_by_id`. One approved version per organisation at a time; an approved row is refused on update at the service and the model | Tests for point-in-time resolution, for refusal to edit an approved version, and for exactly one approved version per organisation | Active |
| SEC-DEC-066 | 2026-08-24 | R1.4 | **`privacy_correction_notes` is append-only, enforced by `BEFORE UPDATE` and `BEFORE DELETE` database triggers, and those triggers are part of GATE 4 ACCEPTANCE - not an indefinite carry-forward** | Decision D11, approved WITH CHANGE 24 August 2026. My proposal was model hooks in gate 4 with triggers recorded as outstanding; the product owner required the database protection designed now. The reasoning is SEC-DEC-037's: model hooks do not fire on a mass delete, and MySQL has no DENY so privileges cannot express this. A correction note is the record of what a data subject disputed - if it can be edited, the dispute can be rewritten by the party being disputed. **`audit_events` shows what the alternative costs**: its triggers took a manual production application and the DELETE half is still unproved | Exact `CREATE TRIGGER` and recreation statements written into the gate 4 release procedure, with model-level hooks as well. **Database triggers are authoritative**; the hooks are defence in depth and not the control. Triggers stay out of migrations for SEC-DEC-037's reason - a migration that creates them could also drop them - so they are a separately controlled production step, and SEC-DEC-039's rebuild constraint applies to this table too | A model-level append-only test in CI, and a production proof recorded in the gate 4 verification document before gate 4 is accepted | Active. **Gate 4 is not accepted until the triggers exist on production and the proof is recorded** |
| SEC-DEC-067 | 2026-08-24 | R1.4 | **Governance authority is split three ways - READ at Domain Owner or an eligible Auditor, MANAGE at Administrator, high-risk APPROVE and RELEASE at System Administrator - and a requester never approves their own request** | Decision D13, approved 24 August 2026. Gate 3 put everything at System Administrator, which is safe and unusable: a Domain Owner who cannot read the retention policy governing their own domain will work around it. But manage and approve collapsing into one tier is how somebody weakens a sovereignty profile and blesses their own weakening. Separation of duties is the control, not the tier number | Three permission suffixes per feature. `admin.sovereignty_exceptions.request` at Administrator and `.approve` at System Administrator, with a service-level check that the approver is not the requester. **No governance permission names or implies a business domain** - the gate 2 rule holds, and an Auditor or Domain Owner reading governance still holds no Finance, Sales or People data | Tests for each tier boundary, for a requester refused approval of their own exception, and for a governance permission granting no domain entitlement | Active |
| SEC-DEC-068 | 2026-08-24 | R1.4 | **The sovereignty profile is seeded from confirmed production facts as a DRAFT, and a draft is never presented as approved** | Decision D12, approved 24 August 2026. Singapore hosting, Singapore backups and no external replication are VERIFIED facts (SEC-DEC-036), so making an administrator retype them invites a typo into the record. But a profile nobody approved is a guess with good provenance, and a screen that showed it as settled would be the false-healthy state SEC-DEC-059 forbids, applied to sovereignty | Created on first visit by the service rather than by a seeder migration, status `draft`, citing SEC-DEC-036 as its source. The screen states plainly that it is unapproved and names who must approve it. Nothing downstream - overlays, exception baselines, the Governance Overview - treats a draft as in force | Tests that the seeded profile is draft, that the overview does not read Healthy on a draft, and that no downstream check honours it | **Discharged 24 August 2026.** The seed was created as a draft on production, the screen reported Not Configured, and the product owner approved it deliberately with a stated reason. The constraint held; the mechanism stays in place for any future organisation |
| SEC-DEC-069 | 2026-08-24 | R1.4 | **Gate 4 produces no export and no generated file, and introduces no queue, worker, scheduler or mail dependency. Deadlines are computed on read** | Decisions D9 and D10, approved 24 August 2026. An export of a subject access response is a durable file full of one person's personal data, with its own retention, access and geography problem, and it would drag in `public/storage` (not linked) and a mail transport (`MAIL_MAILER=log`) - three unapproved dependencies bought at once. Active reminders would add a fourth: production runs `QUEUE_CONNECTION=sync` with no worker. Computing on read needs none of them and cannot go stale | Responses are reviewed on screen and delivered outside the application, with an external evidence reference recorded against the request. Deadline and overdue state derived at render time from the frozen due date | Tests asserting no file is written and no mail is queued by any gate 4 path | Active. **Export and active reminders are each a separate approval and bring their dependencies with them** |
| SEC-DEC-070 | 2026-08-24 | R1.4 | **A privacy request can be raised for a person with no SemantIQ account, and identity verification is mandatory before anything is released** | Decision D6, approved 24 August 2026. A contractor whose account was deleted still has personal data in `audit_events`, and the PDPA does not stop applying because the account did. But a request keyed to nobody is also the obvious attack: assemble a stranger's data by asserting you are them. Verification is what carries the weight that an authenticated session would otherwise carry | `subject_user_id` nullable; the request carries the subject's name and contact details independently. Release is refused until identity verification is recorded, with who verified, when and against what | Tests that an unverified request cannot be released, and that a request with no linked account still assembles correctly | Active |
| SEC-DEC-071 | 2026-08-24 | R1.4 | **The PDPC notification deadline defaults to 3 calendar days, is configurable, and its legal basis is compliance-owned text stored on the profile. The computed deadline is FROZEN on the breach record when the notification decision is made** | Decision D7, approved 24 August 2026. Engineering may implement the field and the default; it is not the source of the legal figure. Freezing matters because a deadline recomputed from a later-edited profile would silently move a date somebody is being held to - the record must say what the deadline WAS when the decision was taken | `pdpc_notification_due_days` and a free-text `basis` on the data protection profile; the resolved due date copied onto the incident at decision time and never recomputed | Tests that changing the profile afterwards does not move an existing incident's deadline | Active. **The figure is accepted for implementation; the basis text remains compliance-owned** |
| SEC-DEC-072 | 2026-08-24 | R1.4 | **A register of real events shows "Migration required" when its storage is absent. It never falls back to a default and never renders as empty** | Extends SEC-DEC-057 to gate 4 and was restated by the product owner as a deployment-safety requirement. A profile has a meaningful secure default - with no override table, the secure default IS what is in force. A register of privacy requests or breaches does not: inventing an empty one says "no incidents" at exactly the moment the screen cannot see whether there are any | `GovernanceStorage`, per table, `Schema::hasTable()`, memoised, singleton, **never a caught database exception** (SEC-DEC-056). Profile and retention reads fall back to the catalogue; privacy request and breach reads report Migration required; all writes fail closed | A gate 4 deployment-order test walking every governance route with the tables dropped, in the shape of `DeploymentOrderTest` | Active |
| SEC-DEC-073 | 2026-08-24 | R1.4a | **A governance form with no draft open renders from the version IN FORCE, not blank** | Found by driving the screen in a browser, and the service test passed throughout. With no draft the form rendered empty, so somebody changing one field posted six empty ones on top of an approved profile and the new version silently lost every value the old one had. `saveDraft()` does copy the approved version before filling - but the copy was overwritten a moment later by the blanks the form had just sent. **The unit test missed it because it called `saveDraft()` with one key rather than through a form.** This is the same lesson as SEC-DEC-061 with a different shape: a service test exercises the service, not the screen | The controllers pass an explicit `editing` value, `$draft ?? $inForce`, and every form field reads from it. The panel also states what saving will do, so a form pre-filled from an approved version does not read as an edit of it | `GovernanceProfileTest::the_form_is_prefilled_from_the_version_in_force_when_no_draft_is_open`, asserted against the rendered HTML rather than the service | Active |
| SEC-DEC-074 | 2026-08-24 | R1.4a | **A sovereignty profile can still be revised after it is approved** | Also found in a browser. The screen rendered its form only when a draft existed; the seeded draft is consumed by the first approval and nothing opened another, so the screen went permanently read-only the moment somebody approved version 1 - with a `beginRevision()` method nothing ever called. A sovereignty position that cannot be changed after approval is worse than useless, because geographies move | The form renders whenever there is something to edit; saving it opens the next version through `saveDraft()`. The approve panel stays gated on an actual draft, because approving nothing is not a state | `GovernanceProfileTest::a_sovereignty_profile_can_still_be_revised_after_it_is_approved` | Active |
| SEC-DEC-075 | 2026-08-24 | R1.4a | **`organisations.privacy_contact` is kept in the database and no longer accepted from a form** | It is the source the structured backfill read from, and dropping the column in the same release would leave nothing to compare against if the backfill took the wrong thing. But leaving it in the validated field list meant a posted value could overwrite the one copy of the thing it exists to preserve. Read-only is the state that matches what it is for | Removed from `OrganisationController::update()`'s rules; still summarised into the audit trail so the legacy value stays visible. The column is dropped only by a later, separately approved migration | `PrivacyContactTest::the_legacy_free_text_field_cannot_be_overwritten_by_a_post` | Active |
| SEC-DEC-076 | 2026-08-24 | R1.4a | **The `.alert-warning` style was missing while seven screens already used it, so every advisory rendered RED** | The base `.alert` defaults to the danger palette and only `.alert-success` and `.alert-info` were defined. "The migration has not run yet" and "three credentials expire this month" both looked like errors on the live site. An advisory that reads as a failure is either ignored or acted on in a panic, and neither is what it was written for. Found while checking the classes a new screen used actually exist | One rule mapping to the existing `--badge-warning-*` tokens, exactly as the two rules above it map to their own. **Invents no token and no colour** - it is the same amber the warning badge already uses | `GovernanceCatalogueTest::the_warning_alert_variant_is_defined`, plus `::every_badge_class_the_enums_return_exists_in_the_stylesheet` which stops the same class of gap recurring | Active |
| SEC-DEC-077 | 2026-08-24 | R1.4a | **`OrganisationController::update()` returned 500 on a post that omitted an optional field** | A `nullable` rule that is not posted at all leaves its key ABSENT from the validated array rather than present and null, and two fields were indexed directly. Any client omitting `default_currency` or `primary_country` got "Undefined array key" and a 500. Pre-existing, and surfaced by a gate 4 test that posted only the fields it was asserting about | `?? null` on both, rather than `=== null` | Covered by `PrivacyContactTest`, whose posts omit both fields | Fixed |
