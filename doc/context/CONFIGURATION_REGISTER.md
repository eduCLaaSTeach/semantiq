# Configuration Register

Behaviour-changing configuration is typed, validated, scoped and documented. Secrets live only in the server environment or an approved secret manager; this register holds metadata, never values.

| Config ID | Key | Scope | Type | Default | Required | Secret | Security / residency impact | Source of truth | Status |
|---|---|---|---|---|---|---|---|---|---|
| CFG-APP-001 | `APP_NAME` | Application | string | SemantIQ | Yes | No | Shown in the top bar and the browser title | Server `.env` | Implemented |
| CFG-BRAND-001 | `brand.assets_path` | Application | string | `/brand` | Yes | No | Where the approved logo and favicon pack is served from | `config/brand.php` | Implemented |
| CFG-AUTH-001 | `services.microsoft.tenant` / `client_id` / `redirect` | Application | string | none | For SSO | No | Must match the Entra app registration exactly | Server `.env` | Implemented, unset on the server |
| CFG-AUTH-002 | `services.microsoft.client_secret` | Application | string | none | For SSO | **Yes** | Compromise permits impersonation of the application. Never committed, never sent to the browser, never logged | Server `.env` | Implemented, unset on the server |
| CFG-NAV-001 | `navigation.policies` | Application | map | see file | Yes | No | Defines the minimum tier, Auditor acceptance and domain requirement for every gated node and route | `config/navigation.php` | Implemented |
| CFG-NAV-002 | `navigation.clusters` | Application | tree | see file | Yes | No | The rail, generated from `doc/MENU_STRUCTURE.md` and mapped onto the four fixed clusters | `config/navigation.php` | Implemented |
| CFG-SOV-001 | Approved storage and processing geographies | Organisation | list | unset | Before production | No | Unset blocks production activation | Not yet built | Planned, Phase 02 |
| CFG-PLAT-001 | `platform.settings` | Application | catalogue | see file | Yes | No | The contract for every runtime setting: key, category, type, default, validation, editable tier, audit and sensitivity. A key not declared here cannot be read or written | `config/platform.php` | Implemented |
| CFG-PLAT-002 | `platform.flags` | Application | catalogue | see file | Yes | No | The contract for every capability flag, including the precondition that makes a switch safe in one direction only. An undeclared flag reads OFF | `config/platform.php` | Implemented |
| CFG-PLAT-003 | `platform.bootstrap_organisation_code` | Application | string | `PRIMARY` | Yes | No | Names the organisation row the bootstrap migration creates. ADM-002 edits that row, never creates a second | `config/platform.php` | Implemented |
| CFG-SET-001 | `app.display_name`, `app.support_contact`, `app.default_locale`, `app.default_time_zone`, `app.pagination_default`, `notifications.default_channel` | Organisation | typed | see catalogue | No | No | Presentation and list defaults. `support_contact` is shown to signed-in people, so it must be a monitored team address | `system_settings` overriding `config/platform.php` | Implemented |
| CFG-SET-002 | `environment.label`, `environment.maintenance_mode`, `environment.maintenance_message` | Organisation | typed | empty / false / empty | No | No | The maintenance message is shown to every signed-in person and must carry no incident detail, host name or customer information | `system_settings` overriding `config/platform.php` | Implemented |
| CFG-FLAG-001 | `identity.local_sign_in` | Organisation | boolean | true | No | No | Offers the email and password form. Turning it OFF requires Microsoft sign-in to be configured, or the instance would have no way in | `feature_flags` overriding `config/platform.php` | Implemented |
| CFG-FLAG-002 | `platform.extended_diagnostics` | Organisation | boolean | false | No | No | Adds driver names and runtime detail to Diagnostics. No credential and no customer data, but it describes the host, so it is off by default | `feature_flags` overriding `config/platform.php` | Implemented |
| CFG-NAV-003 | `navigation.policies.*.permission` | Application | string | none | No | No | Lets a rail node name a declared permission as well as a tier. Both must hold. An undeclared permission denies, so a typo removes a branch of the rail rather than opening one | `config/navigation.php` | Implemented |
| CFG-PERM-001 | `PermissionRegistry` catalogue | Application | code | see file | Yes | No | The contract for every permission: key, module, ceiling, auto-grant tier, risk and audit requirement. **Not configuration data** - it is code, because a permission nothing checks must not be creatable | `app/Modules/Identity/Support/PermissionRegistry.php` | Implemented |
| CFG-QUEUE-001 | `QUEUE_CONNECTION` | Application | string | `sync` on production | Yes | No | **`sync` runs queued work inline in the web request.** Nothing is queued in gates 1 and 2, so it is harmless today. ADM-022 Background Jobs in gate 6 needs a real driver and a running worker, or the Background Jobs screen has nothing to show and long operations will block a web request | Server `.env`, template in `.github/workflows/deploy.yml` | Implemented as `sync`. **Revisit before gate 6** |
| CFG-SCHED-001 | Server cron running `schedule:run` | Server | cron entry | every minute | Yes | No | Without it no scheduled work happens and nothing errors - the failure is silent. The heartbeat exists so the Platform Overview can say whether it is running | cPanel Cron Jobs | **Installed and confirmed healthy, 25 August 2026** |
| CFG-SEC-001 | `security.policies` | Application | catalogue | see file | Yes | No | The contract for every security policy: key, screen, type, default, validation, editing tier, whether a reason is compulsory and which environment capability it needs. A key not declared here cannot be read or written. **Key prefixes avoid the words "auth" and "session"** because the audit redactor replaces the value of any key containing either, which would record every policy change as "[redacted]" | `config/security.php` | Implemented |
| CFG-SEC-002 | `security.screens` | Application | map | three entries | Yes | No | The closed list of policy screens. A group not named here cannot become a URL | `config/security.php` | Implemented |
| CFG-SEC-003 | `security.critical_actions` | Application | list | four entries | Yes | No | Which actions demand a fresh proof of identity. Four of ADM-010's six; the other two guard actions that do not exist until gates 4 and 5, and a switch protecting nothing is indistinguishable from a control that has silently failed | `config/security.php` | Implemented |
| CFG-SEC-004 | `sign_in.mode`, `sign_in.allow_local_admin`, `sign_in.require_federated_for_business_users`, `sign_in.auto_create_users`, `sign_in.allowed_tenant_id`, `sign_in.allowed_email_domains`, `sign_in.failed_attempt_threshold`, `sign_in.lock_minutes` | Organisation | typed | Entra with local admin / true / true / **false** / empty / empty / 5 / 1 | No | No | ADM-009. **The default mode refuses the credential form to any local account below System Administrator**, which is what requiring Entra for business users means and is the change most likely to surprise somebody after a deployment. The screen states how many accounts that is | `security_policies` overriding `config/security.php` | Implemented |
| CFG-SEC-005 | `activity.idle_minutes`, `activity.maximum_minutes`, `activity.remember_me_days`, `activity.concurrent_policy`, `activity.concurrent_limit`, `activity.revocation_enabled`, `activity.confirm_critical_actions`, `activity.confirmation_valid_minutes` | Organisation | typed | 120 / 720 / **0** / unlimited / 3 / true / true / 15 | No | No | ADM-010. Remember-me is off by default: a remembered sign-in outlives the maximum session duration and is a credential sitting on a device. The three capability-dependent keys are stored and NOT in force while the session driver is `file` | `security_policies` overriding `config/security.php` | Implemented |
| CFG-SEC-006 | `api.security_headers`, `api.content_policy_mode`, `api.hsts_enabled`, `api.hsts_max_age_days`, `api.max_payload_kilobytes`, `api.sensitive_rate_limit_per_minute` | Organisation | typed | true / **report_only** / **false** / 1 / 2048 / 30 | No | No | ADM-011. **HSTS is OFF and must stay off until separately approved for production** - it cannot be withdrawn from a browser that has already seen it, so a wrong value is an outage nobody can shorten. Its duration defaults to one day rather than a year for the same reason. The Content Security Policy ships report-only so an administrator can read what it would block before it blocks anything | `security_policies` overriding `config/security.php` | Implemented |
| CFG-SESSION-001 | `SESSION_DRIVER` | Application | string | `file` on production | Yes | No | **`file` sessions cannot be listed by user**, so ADM-010's session revocation and concurrent session limits cannot run. The `sessions` table with its `user_id` column already exists; `database` is what makes both work. Switching signs everybody out at the moment it takes effect, so it is a separately approved change and NOT part of gate 3 | Server `.env`, template in `.github/workflows/deploy.yml` | Implemented as `file`. **Decision D3: build now, switch later under separate approval** |
| CFG-FS-001 | `filesystems.disks.local.visibility` | Application | string | `private` | Yes | No | Laravel's `storage.local` routes carry no middleware and are gated by a signed URL INSIDE the handler - but only while the disk is private. Setting this to `public` turns that check off and makes those routes genuinely anonymous. The ADM-011 authentication control checks the condition rather than assuming it | `config/filesystems.php` | Implemented as `private` |
| CFG-DEPLOY-001 | Deployment order: code ships before migrations run | Application | behaviour | code-first | Yes | No | **The deploy workflow does not run migrations, deliberately** - a deployment that migrates a production database unattended can lose data. Every release adding a table therefore opens a window in which the code is live and the table is not. Gate 3 middleware runs on the WEB stack, so before `SecurityStorage` existed the first request after a deploy returned 500 on `/sign-in`. Any later gate adding a table read from the request path must carry the same readiness fallback | `.github/workflows/deploy.yml`, `App\Modules\Security\Support\SecurityStorage` | **Unchanged by R1.3.** Making the workflow migrate automatically is an architecture change requiring separate approval |
