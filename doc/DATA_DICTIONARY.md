# SemantIQ Data Dictionary

**GENERATED. Do not edit this file by hand.**

Regenerate with:

```text
php tools/generate-data-docs.php
```

Structure comes from `database/migrations/`, which CLAUDE.md names as the
source of truth for the schema. Existence, keys and indexes come from the live
database. The two are cross-checked and the generator REFUSES TO WRITE if they
disagree, so a column documented here is a column that exists.

Meaning comes from `tools/data-column-meanings.php` and `tools/data-meanings.php`,
which are hand written. That is the one part no machine can supply, and it is
the part worth reading.

Types are shown as **MySQL** produces them, because production is MySQL.

Covers 31 tables, 383 columns, 84 foreign keys.

---

## How to read this

| Column | What it tells you |
|---|---|
| **Column** | The column name. Bold means it is a primary key |
| **Type** | The MySQL type |
| **Null** | `-` means NOT NULL. `yes` means the column may be empty, and the meaning column says what empty means |
| **Default** | What a row gets when nothing is supplied |
| **Points at** | The foreign key target, and what happens when the parent goes |
| **What it means** | Why the column exists and what a reader should know about it |

Two conventions run through the whole schema and are worth knowing before you
read any table:

**`organisation_id` is the tenancy boundary.** Almost every table has one, and
the `BelongsToOrganisation` trait applies a global scope that matches NOTHING
when no organisation context is resolved - not everything. The failure mode of a
missing scope is an empty screen somebody investigates, never another
customer's data.

**`*_by_user_id` columns are `nullOnDelete`.** The record survives the person
leaving. An approval that happened still happened, and losing who made it would
be worse than showing a departed account.

---

## Contents

**Identity** - [access_review_items](#access-review-items), [access_reviews](#access-reviews), [business_units](#business-units), [domain_entitlements](#domain-entitlements), [organisations](#organisations), [role_permissions](#role-permissions), [roles](#roles), [teams](#teams), [user_roles](#user-roles), [users](#users)

**Audit** - [audit_events](#audit-events)

**Platform** - [feature_flags](#feature-flags), [system_settings](#system-settings)

**Security** - [secret_references](#secret-references), [security_policies](#security-policies)

**Governance** - [data_protection_profiles](#data-protection-profiles), [data_sovereignty_profiles](#data-sovereignty-profiles), [personal_data_categories](#personal-data-categories), [privacy_correction_notes](#privacy-correction-notes), [privacy_request_records](#privacy-request-records), [privacy_requests](#privacy-requests), [retention_policies](#retention-policies), [sovereignty_exceptions](#sovereignty-exceptions)

**Laravel** - [cache](#cache), [cache_locks](#cache-locks), [failed_jobs](#failed-jobs), [job_batches](#job-batches), [jobs](#jobs), [password_reset_tokens](#password-reset-tokens), [sessions](#sessions)

---

# Identity

## `access_review_items`

One thing being reviewed: a person's role, tier or entitlement, and the decision taken.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Confidential |
| **Personal data** | Yes - about a person |
| **Columns** | 13 |

Revocations are applied through `UserRegistry`, never by direct delete - a bulk operation that bypasses the rules is how a bulk operation becomes the way around the rules (SEC-DEC-030).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `access_review_id` | `bigint unsigned` | - | - | `access_reviews.id (cascade)` | Which review this item belongs to. |
| `user_id` | `bigint unsigned` | - | - | `users.id (cascade)` | Whose access is being reviewed. |
| `subject_type` | `varchar(16)` | - | - | - | What is under review: a role, a tier, an entitlement. |
| `subject_key` | `varchar(64)` | - | - | - | Which specific one. |
| `subject_label` | `varchar(190)` | - | - | - | The subject as text at the time of the review, so the item still reads correctly after the underlying thing changes. |
| `decision` | `varchar(16)` | - | `pending` | - | Keep, revoke, or undecided. **Undecided is never treated as an implicit approval** (SEC-DEC-031). |
| `note` | `varchar(500)` | yes | - | - | Why the reviewer decided that. |
| `decided_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who decided. |
| `decided_at` | `timestamp` | yes | - | - | When. |
| `applied` | `tinyint(1)` | - | `0` | - | Whether the decision has been carried out yet. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `access_reviews`

A periodic examination of who holds what.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Internal |
| **Personal data** | Indirectly |
| **Columns** | 14 |

Cannot be completed while any item is undecided: an ignored item is a finding, and folding it into the same shape as a finished one would hide exactly that (SEC-DEC-031).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `description` | `varchar(500)` | yes | - | - | Free text, shown to a reader. |
| `status` | `varchar(24)` | - | `draft` | - | Lifecycle state, from a codified list. Never free text. |
| `due_at` | `date` | yes | - | - | When the review must be finished by. |
| `opened_at` | `timestamp` | yes | - | - | When it started. |
| `completed_at` | `timestamp` | yes | - | - | When every item had been decided. |
| `applied_at` | `timestamp` | yes | - | - | When the decisions were carried out. Distinct from completion: deciding and doing are two acts. |
| `opened_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who opened it. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `business_units`

The organisation's internal structure, as a hierarchy.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Internal |
| **Personal data** | Incidental - names a manager |
| **Columns** | 15 |

`parent_id` is self-referencing. Effective dating means a unit that closed stays readable rather than being deleted.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `code` | `varchar(32)` | - | - | - | Stable machine identifier. Not mass-assignable, because changing one silently breaks whatever resolves against it. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `parent_id` | `bigint unsigned` | yes | - | `business_units.id (set null)` | The unit above this one. Self-referencing; null at the top. |
| `manager_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who runs it. |
| `cost_centre` | `varchar(64)` | yes | - | - | The customer's own cost centre reference. |
| `country` | `varchar(2)` | yes | - | - | Where the unit operates. |
| `effective_from` | `date` | yes | - | - | When it came into being. |
| `effective_to` | `date` | yes | - | - | When it closed. Null means still open. **Closed rather than deleted**, so history stays readable. |
| `status` | `varchar(24)` | - | `active` | - | Lifecycle state, from a codified list. Never free text. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `domain_entitlements`

One person's access to one business domain. **The second access dimension.**

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | P00 |
| **Classification** | Confidential |
| **Personal data** | Yes - it is about a person |
| **Columns** | 7 |

ROLE_MODEL.md section 1: a platform role NEVER implies business data. A System Administrator with no row here reads no Finance figure. This table is what makes that true.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `user_id` | `bigint unsigned` | - | - | `users.id (cascade)` | Foreign key. See the relationships table below. |
| `domain` | `varchar(32)` | - | - | - | Which business domain: Executive, Sales, Finance, People, Operations, Customer, Learning. |
| `scope` | `json` | yes | - | - | Any narrowing - a region, a legal entity, a cost centre. |
| `granted_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who granted it. A domain grant is a separate decision from a tier change, with its own route, form and audit event (SEC-DEC-029). |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `organisations`

The customer organisation that owns this instance. Every scoped record points at it.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.1, R1.2, R1.4a |
| **Classification** | Internal |
| **Personal data** | Yes - the three named contacts |
| **Columns** | 23 |

One row today. The product stays multi-tenant-ready, so no code assumes there is only one. `code` is immutable once dependencies exist (VAL-ORG-CODE-001) and is deliberately not mass-assignable.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `code` | `varchar(32)` | - | - | - | Stable machine identifier. Not mass-assignable, because changing one silently breaks whatever resolves against it. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `status` | `varchar(24)` | - | `active` | - | Lifecycle state, from a codified list. Never free text. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `version` | `int unsigned` | - | `1` | - | Optimistic concurrency counter, or the version number of an immutable record - see the table notes. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |
| `legal_name` | `varchar(255)` | yes | - | - | The registered name, if it differs from the trading name. |
| `registration_number` | `varchar(64)` | yes | - | - | The customer's own company or entity registration reference. |
| `primary_country` | `varchar(2)` | yes | - | - | ISO 3166-1 alpha-2. Decides which privacy and residency questions apply; does not answer them. |
| `primary_domain` | `varchar(190)` | yes | - | - | The email domain the organisation uses. |
| `default_time_zone` | `varchar(64)` | yes | - | - | IANA identifier. Stored data stays UTC. |
| `default_currency` | `varchar(3)` | yes | - | - | ISO 4217, three letters, uppercased on the way in. |
| `default_language` | `varchar(16)` | yes | - | - | Display language preference. |
| `data_owner` | `varchar(190)` | yes | - | - | Who is accountable for this organisation's data. **A name or a job title, never a login or a key.** |
| `privacy_contact` | `varchar(190)` | yes | - | - | **Legacy, read-only.** The single free-text field that preceded the structured contact. Kept as the source the R1.4a backfill read from, so a wrong backfill can still be compared against the original. No longer accepted from a form (SEC-DEC-075). |
| `security_contact` | `varchar(190)` | yes | - | - | Who to reach about a security question. A name or a role. |
| `privacy_contact_name` | `varchar(190)` | yes | - | - | **Required.** The designated privacy contact. A name or a job title. |
| `privacy_contact_email` | `varchar(190)` | yes | - | - | **Required.** Where a data subject or a regulator can reach that person. A shared mailbox is usually better than one person's inbox. |
| `privacy_contact_phone` | `varchar(64)` | yes | - | - | Optional. |
| `privacy_contact_role` | `varchar(190)` | yes | - | - | Optional. For example Data Protection Officer. |

## `role_permissions`

Which declared permission keys a role carries.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 6 |

There is NO `permissions` table. The catalogue is code, in `PermissionRegistry` - a permission an administrator can invent is a permission nothing checks (SEC-DEC-028). A key here that no longer exists in the registry grants nothing.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `role_id` | `bigint unsigned` | - | - | `roles.id (cascade)` | Foreign key. See the relationships table below. |
| `permission_key` | `varchar(96)` | - | - | - | A key from `PermissionRegistry`. Validated on the way in AND on the way out, so a row that outlived its declaration grants nothing. |
| `granted_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who added this permission to the role. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `roles`

A named bundle of permissions, assignable to people.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 13 |

Six built-in roles ship with `is_system = 1` and `organisation_id IS NULL`. They carry NO `role_permissions` rows: a built-in role derives its permissions from its tier at runtime. A custom role belongs to one organisation.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `code` | `varchar(64)` | - | - | - | Stable machine identifier. Not mass-assignable, because changing one silently breaks whatever resolves against it. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `description` | `varchar(500)` | yes | - | - | Free text, shown to a reader. |
| `tier` | `varchar(32)` | - | - | - | The platform tier this role is meant for. A role cannot lift its holder above their own tier. |
| `is_system` | `tinyint(1)` | - | `0` | - | Whether this is one of the six built-in roles. A built-in role cannot be edited or deleted. |
| `status` | `varchar(24)` | - | `active` | - | Lifecycle state, from a codified list. Never free text. |
| `version` | `int unsigned` | - | `1` | - | Optimistic concurrency counter, or the version number of an immutable record - see the table notes. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `teams`

A team inside a business unit.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Internal |
| **Personal data** | Incidental - names a lead |
| **Columns** | 12 |

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `business_unit_id` | `bigint unsigned` | - | - | `business_units.id (restrict)` | Which unit the team belongs to. |
| `code` | `varchar(32)` | - | - | - | Stable machine identifier. Not mass-assignable, because changing one silently breaks whatever resolves against it. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `description` | `varchar(500)` | yes | - | - | Free text, shown to a reader. |
| `lead_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who leads it. |
| `status` | `varchar(24)` | - | `active` | - | Lifecycle state, from a codified list. Never free text. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `user_roles`

Which roles a person has been assigned.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | R1.2 |
| **Classification** | Confidential |
| **Personal data** | Yes - it is about a person |
| **Columns** | 6 |

**Cannot become a privilege-escalation path.** The tier ceiling is applied AFTER the union of tier defaults and role grants, so a role carrying a permission above its holder's tier is inert (SEC-DEC-023).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `user_id` | `bigint unsigned` | - | - | `users.id (cascade)` | Foreign key. See the relationships table below. |
| `role_id` | `bigint unsigned` | - | - | `roles.id (cascade)` | Foreign key. See the relationships table below. |
| `assigned_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who assigned the role. **Nobody may hand out authority they do not hold themselves** (VAL-USER-ELEVATE-001). |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `users`

A person who can sign in. Carries the platform tier, the federated identity link, the organisation placement and the access window.

| | |
|---|---|
| **Module** | Identity |
| **Introduced** | P00, R1.1, R1.2 |
| **Classification** | Confidential |
| **Personal data** | Yes - name, email, sign-in times |
| **Columns** | 22 |

The ONLY scoped table with no global organisation scope. SEC-DEC-022: it is loaded by id on every request, and a fail-closed global scope there would mean nobody can sign in, including the administrator who would fix it. Tenancy is enforced explicitly by `User::scopeInCurrentOrganisation()` and by `UserRegistry::assertInOrganisation()` on every mutation.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `name` | `varchar(255)` | - | - | - | Display name. From the directory on a federated account. |
| `email` | `varchar(255)` | - | - | - | Work email. Unique, and the identifier a person signs in with. |
| `email_verified_at` | `timestamp` | yes | - | - | Framework column. Unused: verification comes from the directory. |
| `password` | `varchar(255)` | yes | - | - | Bcrypt hash, or null. **Null is normal** - a federated account has no password here, which is the point. |
| `remember_token` | `varchar(100)` | - | - | - | Framework "remember me" token. `activity.remember_me_days` ships at 0, so nothing issues one. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |
| `entra_object_id` | `varchar(64)` | yes | - | - | The Microsoft Entra object id linking this account to a directory account. Not a credential. |
| `entra_tenant_id` | `varchar(64)` | yes | - | - | The Entra tenant the account came from. Checked against the allow-list at sign-in. |
| `last_signed_in_at` | `timestamp` | yes | - | - | When they last got in. Drives the dormant-account view. |
| `role` | `varchar(32)` | - | `Role::default(` | - | The platform tier, one of six cumulative values. **A CEILING that nothing raises** - no role, grant or assignment gets past it. |
| `is_auditor` | `tinyint(1)` | - | `0` | - | The Auditor capability flag. Today understood by `Navigation` only; `Authorization` learns it in R1.4b (decision D2, SEC-DEC-062). |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (set null)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `business_unit_id` | `bigint unsigned` | yes | - | `business_units.id (set null)` | Where they sit in the structure. Optional. |
| `team_id` | `bigint unsigned` | yes | - | `teams.id (set null)` | Which team. Optional. |
| `user_type` | `varchar(16)` | - | `internal` | - | Employee, contractor, service account and so on. Affects expectations, not authority. |
| `status` | `varchar(24)` | - | `active` | - | Invited, active, disabled, locked or expired. **Checked in the authorization layer, not only at sign-in** - a live session can outlive the moment somebody was disabled (SEC-DEC-026). |
| `authentication_source` | `varchar(16)` | - | `local` | - | How they authenticate: the directory, or a local credential. |
| `external_reference_id` | `varchar(64)` | yes | - | - | The customer's own identifier for this person, if they have one. |
| `access_start` | `date` | yes | - | - | First day access is valid. Null means no start bound. |
| `access_end` | `date` | yes | - | - | Last day access is valid. A contractor past this date is refused, and told so on the Microsoft path (SEC-DEC-032). |

---

# Audit

## `audit_events`

What happened, who did it, when, and why. The evidence trail.

| | |
|---|---|
| **Module** | Audit |
| **Introduced** | R1.1 |
| **Classification** | Restricted |
| **Personal data** | Yes - actor identity and IP address |
| **Columns** | 18 |

**APPEND ONLY, enforced at the database.** Two triggers (`BEFORE UPDATE`, `BEFORE DELETE`) raise SQLSTATE 45000. Model hooks throw as well, but the triggers are the control: hooks do not fire on a mass delete, and MySQL has no DENY. SEC-DEC-037. If this table is ever rebuilt the triggers must be re-applied by hand (SEC-DEC-039).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (set null)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `occurred_at` | `timestamp` | - | - | - | When it happened, in UTC. Distinct from `created_at`, which is when the row was written. |
| `actor_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who did it. Null for a system action or an unauthenticated attempt. |
| `actor_type` | `varchar(24)` | - | `user` | - | `user` or `system`. |
| `actor_label` | `varchar(255)` | yes | - | - | The actor as text, captured at the time. Survives the account being deleted, which an id would not. |
| `action` | `varchar(96)` | - | - | - | The catalogue's dotted name, for example `governance.sovereignty_profile.approved`. |
| `module` | `varchar(32)` | - | - | - | Which module wrote it: Identity, Security, Platform, Governance. |
| `resource_type` | `varchar(64)` | yes | - | - | What kind of thing it happened to. |
| `resource_id` | `varchar(64)` | yes | - | - | Which one. Text, so it can hold a non-integer key. |
| `outcome` | `varchar(16)` | - | - | - | Succeeded, failed or denied. **A denial is evidence** and is recorded, not swallowed. |
| `before_summary` | `json` | yes | - | - | Redacted snapshot before the change. Any key matching the sensitive-fragment list stores `[redacted]` instead of its value. |
| `after_summary` | `json` | yes | - | - | Redacted snapshot after the change. **This is why column and key names matter** - a policy stored under an obvious name like `auth.timeout` would record every change as "[redacted] -> [redacted]" (SEC-DEC-044). |
| `reason` | `varchar(512)` | yes | - | - | The written reason, scrubbed. Required on high-risk actions. |
| `ip_address` | `varchar(45)` | yes | - | - | Where the request came from. **Personal data.** From R1.4b it sits behind its own permission, `admin.audit.view_network`, at System Administrator only (SEC-DEC-063). |
| `correlation_id` | `varchar(64)` | yes | - | - | One id per request, carried into the trail, the logs and the response header. An inbound value is accepted only when it is a well-formed UUID. |
| `environment` | `varchar(32)` | - | - | - | Which environment produced the event. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |

---

# Platform

## `feature_flags`

Capability switches. An undeclared flag reads OFF.

| | |
|---|---|
| **Module** | Platform |
| **Introduced** | R1.1 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 8 |

A flag with a precondition refuses the unsafe direction rather than being switched blindly.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `key` | `varchar(96)` | - | - | - | Catalogue key for a capability switch. |
| `enabled` | `tinyint(1)` | - | - | - | Whether it is on. An undeclared flag reads OFF regardless. |
| `reason` | `varchar(512)` | yes | - | - | Why it was switched. Kept because a flag nobody can explain is a flag nobody dares turn off. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `system_settings`

Runtime configuration overrides. The catalogue in `config/platform.php` decides which keys exist.

| | |
|---|---|
| **Module** | Platform |
| **Introduced** | R1.1 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 7 |

Overrides only - a key with no row reads its catalogue default, so a fresh install needs no seeder. `SystemSettings::set()` refuses a secret-bearing key outright.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `key` | `varchar(96)` | - | - | - | Catalogue key. An unknown key throws rather than being stored. |
| `value` | `text` | yes | - | - | The override, as text. Cast back by the catalogue's declared type. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

---

# Security

## `secret_references`

WHERE a credential is kept and when it lapses. **Never the credential.**

| | |
|---|---|
| **Module** | Security |
| **Introduced** | R1.3 |
| **Classification** | Restricted |
| **Personal data** | Incidental - names an owner |
| **Columns** | 16 |

No column could hold a secret; the model refuses a credential-shaped string and the form request refuses one before that. Reading this list is as restricted as writing it: a map of every credential a system depends on is a target list (SEC-DEC-052).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `name` | `varchar(120)` | - | - | - | What a person calls this. |
| `reference_type` | `varchar(40)` | - | - | - | What kind of thing is held elsewhere. **Named `reference_type`, not `secret_type`** - the latter trips the audit redactor. |
| `provider` | `varchar(40)` | - | - | - | Where it lives: the server environment, a key vault, a provider console. |
| `reference_identifier` | `varchar(190)` | - | - | - | How to find it there. A pointer, refused if it looks like a credential itself. |
| `purpose` | `varchar(500)` | - | - | - | What depends on it. This is what makes an expiry date actionable. |
| `environment` | `varchar(40)` | - | - | - | Which environment the credential serves. |
| `owner_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who is responsible for rotating it. |
| `expires_on` | `date` | yes | - | - | When it lapses. Null means nobody is tracking it, which the screen shows rather than hides. |
| `rotation_due_on` | `date` | yes | - | - | When it should be rotated, which is usually sooner than expiry. |
| `retired_at` | `timestamp` | yes | - | - | When it stopped being used. Retired, not deleted. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `security_policies`

Authentication, session and API security policy overrides.

| | |
|---|---|
| **Module** | Security |
| **Introduced** | R1.3 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 8 |

Overrides only. Its own table rather than `system_settings` because every natural key for these features trips the audit redactor (SEC-DEC-046). Key prefixes are `sign_in.`, `activity.` and `api.` for the same reason - `auth.` and `session.` would be redacted out of their own audit trail.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | yes | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `key` | `varchar(120)` | - | - | - | Catalogue key, prefixed `sign_in.`, `activity.` or `api.` - never `auth.` or `session.`, which the audit redactor would swallow. |
| `value` | `text` | yes | - | - | The override, as text. |
| `reason` | `varchar(500)` | yes | - | - | Required on a high-risk policy change. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

---

# Governance

## `data_protection_profiles`

The organisation's stated privacy position, as immutable versions.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4a |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 18 |

**Versioned.** An approved version refuses every update except supersession (SEC-DEC-065). A mutable row cannot answer "what was in force in March", which is what a regulator, an auditor and a breach assessment all ask. The two `*_basis` columns are compliance-owned and ship empty.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `version` | `int unsigned` | - | `1` | - | Optimistic concurrency counter, or the version number of an immutable record - see the table notes. |
| `status` | `varchar(16)` | - | `draft` | - | Lifecycle state, from a codified list. Never free text. |
| `applicable_regime` | `varchar(64)` | yes | - | - | Which privacy law governs this data. A field, not a constant, so a customer in another jurisdiction needs no code change. |
| `regime_basis` | `text` | yes | - | - | **Compliance-owned. Ships empty and SemantIQ will never fill it in.** Why that regime applies: who determined it, when, on what grounds. A plausible sentence generated by software would be a compliance claim nobody made. |
| `privacy_officer_designated` | `tinyint(1)` | - | `0` | - | Whether somebody has actually been appointed. **Not inferred from the contact field being filled in** - a name in a box is not an appointment. |
| `breach_notification_due_days` | `smallint unsigned` | yes | - | - | How long there is to notify the regulator. Ships at 3 calendar days (decision D7). A breach record freezes the resolved date at decision time, so editing this later cannot move a deadline somebody is held to. |
| `breach_notification_basis` | `text` | yes | - | - | **Compliance-owned. Ships empty**, for the same reason as `regime_basis`. |
| `notes` | `text` | yes | - | - | Anything the fields do not cover. |
| `approved_at` | `timestamp` | yes | - | - | When this version was approved. From that moment it is immutable. |
| `approved_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who approved it. `nullOnDelete`: the approval still happened. |
| `superseded_at` | `timestamp` | yes | - | - | When a later version replaced it. |
| `superseded_by_id` | `bigint unsigned` | yes | - | `data_protection_profiles.id (set null)` | Which version replaced it. Self-referencing. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `data_sovereignty_profiles`

Where the organisation stores, processes and backs up its data, as immutable versions.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4a |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 25 |

**Versioned**, same contract as ADM-014. Seeded as a DRAFT from the facts SEC-DEC-036 confirmed. Backups get their own column because backups routinely leave the country the server sits in - folding them into storage would lose the distinction that made the question worth asking three times.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `version` | `int unsigned` | - | `1` | - | Optimistic concurrency counter, or the version number of an immutable record - see the table notes. |
| `status` | `varchar(16)` | - | `draft` | - | Lifecycle state, from a codified list. Never free text. |
| `storage_geography` | `varchar(32)` | yes | - | - | Where the data itself sits. |
| `processing_geography` | `varchar(32)` | yes | - | - | Where it is processed, **which is not always where it sits**. |
| `ai_processing_geography` | `varchar(32)` | yes | - | - | Where prompts, responses and embeddings are processed. `not_determined` until an AI service is provisioned. |
| `backup_geography` | `varchar(32)` | yes | - | - | Where the backups live. **Its own column on purpose**: backups routinely leave the country the server is in, which moves data across a border without the server moving. |
| `approved_geographies` | `json` | yes | - | - | JSON list of geographies this organisation permits at all. **Named `approved_`, never `authorised_`** - `auth` is a redactor fragment and the value would store as `[redacted]`. |
| `external_replication` | `varchar(32)` | yes | - | - | Whether data is replicated outside its geography, and how far. |
| `cross_geo_storage` | `tinyint(1)` | - | `0` | - | Whether data may be stored outside its geography. **Ships OFF at the database default**, per CLAUDE.md. |
| `cross_geo_processing` | `tinyint(1)` | - | `0` | - | Whether it may be processed outside. Ships OFF. |
| `cross_geo_ai` | `tinyint(1)` | - | `0` | - | Whether AI may process it outside. Ships OFF. |
| `cross_geo_conversation_history` | `tinyint(1)` | - | `0` | - | Whether conversation history may be held outside. Ships OFF. |
| `source_note` | `text` | yes | - | - | Where the values came from. Carries the seed's provenance so a reader can tell a confirmed fact from a typed-in one. |
| `evidence_reference` | `varchar(190)` | yes | - | - | A pointer to proof held elsewhere - a hosting contract, a provider attestation. **Named `evidence_`, never `certification_`**: `cert` is a redactor fragment. |
| `notes` | `text` | yes | - | - | Anything the fields do not cover. |
| `approved_at` | `timestamp` | yes | - | - | When this version was approved. Immutable from then on. |
| `approved_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who approved it. |
| `superseded_at` | `timestamp` | yes | - | - | When a later version replaced it. |
| `superseded_by_id` | `bigint unsigned` | yes | - | `data_sovereignty_profiles.id (set null)` | Which version replaced it. Self-referencing. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `personal_data_categories`

What kinds of personal data this application holds about people, and where each kind lives.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4a |
| **Classification** | Internal |
| **Personal data** | No - it DESCRIBES personal data, holds none |
| **Columns** | 13 |

Seeded from a scan of the live schema, not a privacy template: personal data was found in 19 of 23 tables, not the five DEC-002 named. `source_tables` is the input to the R1.4c collector coverage test. Retired, never deleted.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `code` | `varchar(64)` | - | - | - | Stable machine identifier. Not mass-assignable, because changing one silently breaks whatever resolves against it. |
| `name` | `varchar(190)` | - | - | - | What a person calls this. |
| `description` | `text` | - | - | - | Free text, shown to a reader. |
| `classification` | `varchar(32)` | - | - | - | Public, internal, confidential or restricted. How much harm disclosure would cause. |
| `contains_sensitive` | `tinyint(1)` | - | `0` | - | Whether it includes data most regimes treat with extra care - health, finances, biometrics. **A different question from classification**: one is harm, the other is kind. |
| `source_tables` | `json` | yes | - | - | JSON list of the tables this category lives in. **The input to the R1.4c collector coverage test**, which fails when a schema table is claimed by no category and named in no exclusion list. |
| `status` | `varchar(16)` | - | `active` | - | Lifecycle state, from a codified list. Never free text. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `privacy_correction_notes`

A data subject's assertion that a record about them is wrong, and what was decided about it.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4c-i |
| **Classification** | Confidential |
| **Personal data** | YES - the assertion is the subject's own words about themselves |
| **Columns** | 12 |

**APPEND-ONLY, and the database is what enforces it.** `audit_events` cannot be edited, so when a subject says the trail is wrong the remedy is a new row here linked to the event id: the original is untouched and anyone reading it sees the challenge beside it. Model hooks throw on update and delete, but they are **defence in depth, not the control** - they do not fire on a mass delete or a raw query, and MySQL has no DENY. The control is a pair of BEFORE UPDATE and BEFORE DELETE triggers, deliberately NOT in a migration (SEC-DEC-037: a migration that can create a trigger can also drop it) and installed as a separately approved production step. **SEC-DEC-066 makes those triggers a gate 4 acceptance condition.** **There is no `updated_by_user_id` column** - a table that can never be updated has no use for one, and its presence would imply an update path exists. **`audit_event_id` uses restrictOnDelete**: an annotation must not vanish because something upstream was removed.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `privacy_request_id` | `bigint unsigned` | - | - | `privacy_requests.id (cascade)` | The request this dispute arose in. |
| `audit_event_id` | `bigint unsigned` | yes | - | `audit_events.id (restrict)` | The disputed entry. **restrictOnDelete, not cascade**: an annotation must not vanish because something upstream was removed. Nullable, because a subject may dispute something that is not a single event. |
| `subject_assertion` | `text` | - | - | - | What the person says is wrong, in their own terms. Written once and never editable, which is the entire point - a dispute the disputed party can rewrite is not evidence of anything. |
| `outcome` | `varchar(16)` | - | `noted` | - | noted, applied or refused. **`noted` is a complete and correct outcome**, not a lesser one: where the disputed record is an audit event, the trail cannot be edited and the permanent annotation beside it IS the remedy. |
| `outcome_reason` | `text` | yes | - | - | Why this outcome was reached. Required on every outcome including a correction, because "corrected" alone does not say what was corrected or on what basis. |
| `decided_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who decided. |
| `decided_at` | `timestamp` | yes | - | - | When. Written with the row, since assertion and decision are recorded together - recording one and then the other would mean updating, and this table cannot be updated. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `privacy_request_records`

One collected item in an assembled response, with the band it falls in and how it may be disclosed.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4c-i |
| **Classification** | Confidential |
| **Personal data** | YES - this is the assembled personal data itself |
| **Columns** | 14 |

**These rows are the response.** There is no document and no export; the response is read on screen and delivered by a person. **Stored rather than recomputed** because a response must be reproducible: recomputed at read time it would change as the data changed, and nobody could later say what was actually disclosed on the day. **`band` and `treatment` carry the disclosure model.** Band C is the design problem - "Alice approved Bob's entitlement" is personal data about both, so the default is `describe`, which states the fact without naming the second person (D5). **`detail` is populated only for `include`**, and `CollectedItem` refuses the other combination at construction, so no collector can produce the leaking shape. **Widening a treatment needs a second approver who is not the reviewer**; narrowing does not.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `privacy_request_id` | `bigint unsigned` | - | - | `privacy_requests.id (cascade)` | Which request this belongs to. Cascade is correct here and only here: these rows have no meaning apart from their request, and the evidence that an assembly happened lives in the audit trail. |
| `band` | `varchar(1)` | - | - | - | A, B, C or D. How this item relates to the subject, which decides the default treatment. **Band C is the design problem**: the subject's name on somebody else's record is personal data about two people. |
| `source_table` | `varchar(64)` | - | - | - | Which table it came from. What the coverage test reconciles against the live schema. |
| `collector` | `varchar(190)` | - | - | - | The collector class that produced it. Recorded so a reader can find the code that made a disclosure decision. |
| `treatment` | `varchar(16)` | - | - | - | include, describe or exclude. **`exclude` rows are kept precisely because they disclose nothing** - they are the evidence that a table was considered and deliberately withheld, which is what makes the coverage claim checkable rather than asserted. |
| `summary` | `text` | yes | - | - | What the subject is told, already rendered. For band C it was rendered by a function that never received the other person's identity, so a template mistake cannot leak what was never passed in. |
| `detail` | `json` | yes | - | - | The verbatim structured payload. **Populated only for `include`** - `CollectedItem` refuses the other combination at construction, so no collector can produce a described item that discloses. |
| `occurred_at` | `timestamp` | yes | - | - | When the underlying thing happened, for the time-ordered bands. |
| `reviewer_action` | `varchar(16)` | yes | - | - | kept, narrowed or widened. **Widening needs a second approver who is not the reviewer**; narrowing does not. Being more careful is always one person's call. |
| `reviewer_note` | `text` | yes | - | - | Why a treatment was changed. Required, so nobody can later ask whether a disclosure was considered or accidental. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `privacy_requests`

A request from a person about the personal data held about them, and its lifecycle from arrival to closure.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4c-i |
| **Classification** | Confidential |
| **Personal data** | YES - it identifies the requester directly, by name and email |
| **Columns** | 30 |

**The subject need not have an account.** `subject_user_id` is nullable by decision D6, and the name and email are held on this row independently: a contractor whose account was deleted still has personal data in `audit_events`, and the PDPA does not stop applying because the account did. **That is also the obvious attack** - assemble a stranger's data by asserting you are them - so `identity_verified_at` gates everything downstream, and the service refuses to collect without it independently of the status. **`due_at` is frozen, not derived.** It is written once at verification. A deadline recomputed from a policy somebody later edited would silently move a date a person is being held to. **No file is ever produced** (D9): `evidence_reference` records how a human delivered the response outside SemantIQ.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `reference` | `varchar(32)` | - | - | - | Quotable in correspondence: PR-0001. Derived from the highest existing reference rather than a count, so a deleted row can never cause one to be reused - a reference that pointed at two different requests would be worse than none. |
| `status` | `varchar(32)` | - | `received` | - | Lifecycle state, from a codified list. Never free text. |
| `request_type` | `varchar(16)` | - | - | - | access, correction or withdrawal. Only the last two route through a decision step; an access request is answered by disclosing. |
| `subject_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | The requester's SemantIQ account, if they have one. **Nullable by decision D6**: a person whose account was deleted still has personal data in `audit_events` and is still entitled to ask for it. |
| `subject_name` | `varchar(190)` | - | - | - | Their name, held here rather than read from `users`, so the request survives the deletion of the account it was about. |
| `subject_email` | `varchar(190)` | - | - | - | How they were contacted. Also what `password_reset_tokens` is matched on, since that table is keyed by address rather than by account. |
| `subject_reference` | `varchar(190)` | yes | - | - | An external identifier, where the customer uses one. Optional. |
| `received_at` | `timestamp` | - | - | - | When the request arrived, which may predate this row - a letter recorded a week later is still received on the day it arrived. |
| `received_channel` | `varchar(32)` | yes | - | - | How it arrived: email, post, in person. Free text because the ways a person can ask are not enumerable. |
| `identity_verified_at` | `timestamp` | yes | - | - | **The gate.** Null means unverified, and unverified means nothing is collected. Checked by the service independently of `status`, because a status records where a row got to and a timestamp records that a person actually did something. |
| `identity_verified_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who confirmed it. The accountable human, not a system. |
| `identity_verification_method` | `varchar(64)` | yes | - | - | From the codified list in `config/governance.php`, so "how was this person identified" is comparable across requests rather than a sentence somebody typed. |
| `identity_verification_note` | `text` | yes | - | - | What was actually checked. Required. "Verified" without this is somebody's word for it. |
| `assembled_at` | `timestamp` | yes | - | - | When collection last ran. Re-running replaces the records wholesale rather than appending, so a response never mixes what was true then with what is true now. |
| `assembled_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who ran the collection. **Recorded so separation of duties can be enforced against the person who actually did the work, not against a permission tier.** A System Administrator holds both `.manage` and `.release`, so the tier split alone stopped nobody from assembling a response and then authorising its own disclosure. The service compares this and `reviewed_by_user_id` against the releaser. |
| `reviewed_at` | `timestamp` | yes | - | - | When a reviewer went through the assembled response. |
| `reviewed_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who reviewed it. |
| `decision` | `varchar(16)` | yes | - | - | released or refused. Refusal is a lawful outcome, not a failure. |
| `decision_reason` | `text` | yes | - | - | Required on refusal. Refusing is defensible; refusing without a stated reason is not. |
| `released_at` | `timestamp` | yes | - | - | When disclosure was authorised. A different act from assembling the response, behind its own permission at System Administrator. |
| `released_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who authorised it. Deliberately capable of being a different person from whoever assembled it. |
| `evidence_reference` | `varchar(190)` | yes | - | - | How the response was actually delivered - a postal tracking number, a meeting date. **SemantIQ sends nothing itself** (D9), so without this there is no evidence the person ever received an answer. |
| `closed_at` | `timestamp` | yes | - | - | When it was finished. A closed request is never reopened; a new one is raised instead, so the record of what was disclosed on a date stays exactly as it was. |
| `due_at` | `timestamp` | yes | - | - | **Frozen at verification, never recomputed.** A deadline derived from a policy somebody later edited would silently move a date a person is being held to. Whether it has passed IS derived on read, so nothing needs to run. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `retention_policies`

How long one category of personal data is kept, on what basis, from what event, and what happens at the end.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4b |
| **Classification** | Internal |
| **Personal data** | No - it describes how personal data is treated |
| **Columns** | 18 |

**This table stores policy and executes nothing.** There is no sweep, no job and no deletion path anywhere in gate 4 (SEC-DEC-038). A filled-in row means somebody wrote down an intention and, once approved, that a human agreed it - not that anything acts on it. Every screen says so, because a table full of periods reads as protection, and reading it that way is how an organisation believes it is compliant while nothing happens. **It replaces one blanket seven-year number**, which DEC-002 traced to a gap: the PDPA expects a stated basis per category. **Three columns are compliance-owned and ship empty** - `retention_months`, `basis` and `lawful_basis` are judgements about law, and null means Not Configured, never "forever" and never seven years.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `personal_data_category_id` | `bigint unsigned` | - | - | `personal_data_categories.id (cascade)` | Which category this policy governs. One policy per category: two would be two answers to one question. |
| `retention_months` | `smallint unsigned` | yes | - | - | How long, in months. **NULL means Not Configured** - never "forever", and never the seven years this repository once applied to everything. Compliance-owned; SemantIQ ships it empty. |
| `basis` | `text` | yes | - | - | Why that long - the obligation, guidance or contract it comes from. **Compliance-owned and ships empty.** A plausible sentence written by software would be a compliance claim nobody made. |
| `lawful_basis` | `varchar(190)` | yes | - | - | Which legal ground the data is held under. Also compliance-owned, also empty. |
| `start_event` | `varchar(64)` | yes | - | - | **When the clock starts.** Without this a period is unusable: "three years" from account closure and from record creation are different dates, often years apart. |
| `disposal_action` | `varchar(32)` | yes | - | - | What happens at the end - review, anonymise, archive, delete, retain. **Recorded as an intention. Nothing in SemantIQ carries it out.** |
| `owner` | `varchar(190)` | yes | - | - | Who is accountable for this category. A name or a job title, never a login or a key. |
| `exception_rule` | `text` | yes | - | - | Anything overriding the period - a legal hold, a dispute, a regulatory obligation. Free text because the shape of an exception is not knowable in advance; its existence is what the screen surfaces. |
| `next_review_on` | `date` | yes | - | - | When somebody should look again. The screen flags it once the date passes, derived on read. Nothing else happens. |
| `status` | `varchar(16)` | - | `draft` | - | Lifecycle state, from a codified list. Never free text. |
| `approved_at` | `timestamp` | yes | - | - | When a person agreed the period. **Approving switches nothing on.** Editing afterwards returns the policy to draft, because a period that changed after approval is not the period anybody approved. |
| `approved_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who agreed it. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

## `sovereignty_exceptions`

A recorded, approved, time-bounded departure from the approved sovereignty profile.

| | |
|---|---|
| **Module** | Governance |
| **Introduced** | R1.4b |
| **Classification** | Internal |
| **Personal data** | Incidental - names a requester and an approver |
| **Columns** | 23 |

**The exception never touches the profile.** It sits beside it. An exception that edited `data_sovereignty_profiles` would make the approved position a lie, and a year later would be indistinguishable from somebody having approved a weaker position outright. **Separation of duties is enforced in the service, not by the tier split** - a System Administrator holds both `.request` and `.approve`, so nothing in the permission model stops them deciding their own request, and a person agreeing with themselves is not an approval (SEC-DEC-067). **Expiry is derived, never stored**: `status` records what a person decided, and whether an approved exception is still in force is a question about today's date answered on read, so one lapses at midnight with nothing needing to run (SEC-DEC-069).

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `organisation_id` | `bigint unsigned` | - | - | `organisations.id (cascade)` | Which customer owns this row. The tenancy boundary. `BelongsToOrganisation` fills it at creation and fails closed when no context is resolved. |
| `data_sovereignty_profile_id` | `bigint unsigned` | yes | - | `data_sovereignty_profiles.id (set null)` | Which approved profile version this departs from, captured at request time. Comparing against whatever is current later would misread an exception raised against version 1 as though it had been raised against version 3. |
| `title` | `varchar(190)` | - | - | - | What the exception is, in a line somebody scanning a list will understand. |
| `justification` | `text` | - | - | - | Why it is unavoidable and what was tried instead. **The whole basis on which data is allowed to leave its geography**, and what a reviewer weighs. |
| `aspect` | `varchar(32)` | - | - | - | Which aspect of the profile it departs from - storage, processing, AI processing, backup, replication. A closed list, because an exception to "some stuff" cannot be reviewed or compared against the profile. |
| `requested_geography` | `varchar(32)` | yes | - | - | Where the data would go. **Named `requested_`, never `authorised_`** - `auth` is an audit-redactor fragment and the value would be stored as `[redacted]` for exactly the change an auditor came to read. |
| `scope_note` | `text` | yes | - | - | Which data, and how much. An exception scoped to everything is not an exception. |
| `starts_on` | `date` | yes | - | - | When it begins. Null means from the moment of approval. |
| `ends_on` | `date` | - | - | - | **Required.** An exception with no end date is a permanent change to the sovereignty position wearing the word "exception". It stops applying on this date by itself, computed on read, with nothing needing to run. |
| `status` | `varchar(16)` | - | `requested` | - | Lifecycle state, from a codified list. Never free text. |
| `requested_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who raised it. Compared against the approver, because a requester never approves their own request. |
| `requested_at` | `timestamp` | yes | - | - | When it was raised. |
| `decided_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who approved or rejected it. **The service refuses to write this equal to `requested_by_user_id`** - the control the tier split cannot express, since a System Administrator holds both permissions. |
| `decided_at` | `timestamp` | yes | - | - | When it was decided. |
| `decision_note` | `text` | yes | - | - | Why it was approved or rejected. Required. |
| `revoked_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who ended it early. **A different act from rejection**: revoking ends something that was in force, rejecting refuses something that never was, and the trail has to tell them apart. |
| `revoked_at` | `timestamp` | yes | - | - | When it was revoked. Takes effect immediately - the next request sees it gone. |
| `revocation_reason` | `text` | yes | - | - | Why it was ended early. Required. |
| `created_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who created it. Kept as `nullOnDelete` so the record survives the person leaving. |
| `updated_by_user_id` | `bigint unsigned` | yes | - | `users.id (set null)` | Who last changed it. Same nullability reasoning. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |
| `updated_at` | `timestamp` | yes | - | - | When the row last changed. |

---

# Laravel

## `cache`

Framework cache store.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Internal |
| **Personal data** | Incidental |
| **Columns** | 3 |

Framework table. Contents are whatever the application cached; treated as opaque and excluded from PDPA-01 because it is derived and expiring.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| `key` | `varchar(255)` | - | - | - | Cache key. |
| `value` | `mediumtext` | - | - | - | Serialised cached value. |
| `expiration` | `bigint` | - | - | - | Unix timestamp. |

## `cache_locks`

Atomic lock records for the cache store.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 3 |

Framework table.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| `key` | `varchar(255)` | - | - | - | Lock name. |
| `owner` | `varchar(255)` | - | - | - | Who holds it. |
| `expiration` | `bigint` | - | - | - | Unix timestamp. |

## `failed_jobs`

A job that threw, with its payload and stack trace.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Internal |
| **Personal data** | Incidental - a payload may contain anything |
| **Columns** | 7 |

**Not in use.** See `jobs`. When gate 6 activates the queue, payloads must be assumed to carry personal data.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `uuid` | `varchar(255)` | - | - | - | Unique id for the failure. |
| `connection` | `varchar(255)` | - | - | - | Which queue connection. |
| `queue` | `varchar(255)` | - | - | - | Which queue the job was on when it failed. Framework column. |
| `payload` | `longtext` | - | - | - | The serialised job. **May incidentally contain personal data** if a queued job ever carried any, which is why `failed_jobs` is named in the privacy exclusion register as a known incidental surface rather than silently ignored. Empty on this deployment. |
| `exception` | `longtext` | - | - | - | The stack trace. |
| `failed_at` | `timestamp` | - | - | - | When it gave up. |

## `job_batches`

Progress of a batch of queued jobs.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Internal |
| **Personal data** | No |
| **Columns** | 10 |

**Not in use.** See `jobs`.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| `id` | `varchar(255)` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `name` | `varchar(255)` | - | - | - | What a person calls this. |
| `total_jobs` | `int` | - | - | - | Size of the batch. |
| `pending_jobs` | `int` | - | - | - | How many remain. |
| `failed_jobs` | `int` | - | - | - | How many failed. |
| `failed_job_ids` | `longtext` | - | - | - | Which ones. |
| `options` | `mediumtext` | yes | - | - | Batch options. |
| `cancelled_at` | `int` | yes | - | - | If cancelled, when. |
| `created_at` | `int` | - | - | - | When the row was written. |
| `finished_at` | `int` | yes | - | - | When it completed. |

## `jobs`

Queued jobs awaiting a worker.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Internal |
| **Personal data** | Incidental |
| **Columns** | 7 |

**Not in use.** Production runs `QUEUE_CONNECTION=sync` (CFG-QUEUE-001), so jobs run inline and this table stays empty. Gate 6 changes that.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| **id** | `bigint unsigned AUTO_INCREMENT` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `queue` | `varchar(255)` | - | - | - | Queue name. |
| `payload` | `longtext` | - | - | - | The serialised job. |
| `attempts` | `smallint unsigned` | - | - | - | How many times it has been tried. |
| `reserved_at` | `int unsigned` | yes | - | - | When a worker took it. |
| `available_at` | `int unsigned` | - | - | - | When it becomes runnable. |
| `created_at` | `int unsigned` | - | - | - | When the row was written. |

## `password_reset_tokens`

A short-lived token proving somebody can read an email address.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Restricted |
| **Personal data** | Yes - email address |
| **Columns** | 3 |

Framework table. Held only while in use. In PDPA-01 scope as category `authentication_state`.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| `email` | `varchar(255)` | - | - | - | Who asked for the reset. |
| `token` | `varchar(255)` | - | - | - | Hashed. Single use, short lived. |
| `created_at` | `timestamp` | yes | - | - | When the row was written. |

## `sessions`

Server-side session state, WHEN the driver is `database`.

| | |
|---|---|
| **Module** | Laravel |
| **Introduced** | P00 |
| **Classification** | Restricted |
| **Personal data** | Yes - IP address and user agent |
| **Columns** | 6 |

**Not in use.** Production runs `SESSION_DRIVER=file` (SEC-DEC-049). The table exists; nothing writes to it. If the driver is ever switched, this becomes live personal data and PDPA-01 scope must include it.

| Column | Type | Null | Default | Points at | What it means |
|---|---|---|---|---|---|
| `id` | `varchar(255)` | - | - | - | Surrogate primary key. Auto-incrementing, never reused, never meaningful. |
| `user_id` | `bigint unsigned` | yes | - | - | Whose session. Null for a guest session. |
| `ip_address` | `varchar(45)` | yes | - | - | Personal data, were this table in use. |
| `user_agent` | `text` | yes | - | - | Personal data, were this table in use. |
| `payload` | `longtext` | - | - | - | The serialised session. |
| `last_activity` | `int` | - | - | - | Unix timestamp of the last request. What a database-driver idle timeout would read. |

---

## Tables that exist but are not in use

Four framework tables are created by Laravel and written by nothing in this
deployment. They are listed because an empty table that LOOKS like it should
have rows is a question somebody will eventually ask.

| Table | Why it is empty | What would change that |
|---|---|---|
| `sessions` | Production runs `SESSION_DRIVER=file` (SEC-DEC-049) | Switching the driver to `database`. It then holds IP addresses and user agents, and PDPA-01 scope must include it |
| `jobs` | Production runs `QUEUE_CONNECTION=sync`, so jobs run inline (CFG-QUEUE-001) | Gate 6, which needs a real queue driver and a running worker |
| `job_batches` | Same | Same |
| `failed_jobs` | Same. Nothing is queued, so nothing can fail asynchronously | Same. Payloads must then be assumed to carry personal data |

